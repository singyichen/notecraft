---
title: "機器學習實作系列：第 13 週 - 降維與應用"
description: ""
tags: ["機器學習實作系列"]
createdAt: "2026-11-30"
updatedAt: "2026-11-30"
---

import GeneratedFrame from '@/components/GeneratedFrame.astro'
import MlWeek13ThreeObjectives from '@/components/generated/ml-week13-three-objectives'
import MlWeek13Selection from '@/components/generated/ml-week13-selection'
import MlWeek13ExplainedVariance from '@/components/generated/ml-week13-explained-variance'

# 第 13 週：降維與應用

*Dimensionality Reduction and Applications*

> 本週參考：[《Python Data Science Handbook》](/notes/python-data-science-handbook)
> 本週參考：[《Machine Learning with PyTorch and Scikit-Learn》](/notes/machine-learning-with-pytorch-and-scikit-learn)

## 本週摘要

### 為什麼要降維：欄位多不等於資訊多

一張顧客資料表可能有兩百個欄位，但「消費頻率」「單次金額」「近三個月活躍度」高度相關，其實在重複描述同一件事。維度一多，資料點被拉得越來越稀疏，模型容易記住雜訊而非規律，這就是維度詛咒（curse of dimensionality）。

減維有兩條路：特徵選擇（feature selection）是「挑欄位」，欄位保持原意；**特徵轉換（feature transformation）**是「造新欄位」——把原始欄位線性組合成新座標軸，每個新軸都是舊欄位的加權混合，不再對應單一原始欄位，卻能用更少維度裝下更多資訊。本週三種方法都屬於後者，差別只在它們各自在最大化什麼。

### PCA：最大化資料本身的變異，不看標籤

主成分分析（principal component analysis, PCA）問的是：「只能留一條軸，往哪個方向投影會讓資料點散得最開？」它不用標籤，屬於非監督式。

步驟是先標準化（不做的話「年薪」的範圍會直接輾壓「年資」），再對共變異數矩陣（covariance matrix）做特徵分解（eigendecomposition）。共變異數矩陣描述這團資料「往哪些方向拉伸成什麼形狀」——把散點雲想成橄欖球，特徵向量是長軸、次長軸、短軸的方向，特徵值是各方向的伸展程度。

特徵值由大到小排序、各除以總和，就得到每個主成分的**解釋變異量（explained variance ratio）**；累加畫成階梯圖就能回答「留幾個」，常見做法是取到累積達 90–95%。決定留 k 個後，把前 k 個特徵向量並排成投影矩陣 W，資料乘上去就完成特徵轉換——W 只能從訓練集學，測試集一律 `transform`、不能重新 `fit`。

### PCA 的另一面：壓縮即去雜訊

投影到低維後也能反投影回原維度，得到「只用前 k 個主成分重建的版本」。被丟掉的低變異方向往往正是雜訊：訊號有結構、集中在少數方向，雜訊零散分布在所有方向。所以這趟往返等同一台濾波器，這就是 PCA 的雜訊過濾（noise filtering）應用。

### LDA：最大化類別可分性，把標籤當指南針

線性判別分析（linear discriminant analysis, LDA）的數學骨架跟 PCA 很像，但它是監督式的。它找的不是「資料散得最開」的方向，而是「投影後同類擠得最緊、異類離得最遠」。

做法是算兩個散布矩陣：類內散布矩陣（within-class scatter matrix, S_W）衡量每一類內部的離散程度，類間散布矩陣（between-class scatter matrix, S_B）衡量各類別中心彼此離多遠；解 S_W⁻¹S_B 的特徵值問題，最大特徵值對應的就是最能拉開類別的判別方向。這裡有個硬限制：S_B 由各類均值構成，最多只有 c−1 個非零特徵值，所以 **c 個類別最多降到 c−1 維**。

差別很直接：若類別差異剛好藏在變異量很小的方向（例如兩種紅酒只差在某個微量成分），PCA 會判定它不重要而丟掉，LDA 卻會第一個抓住它。

### 非線性降維：資料躺在一張捲起來的紙上

PCA 與 LDA 都是線性投影，本質是「換一組座標軸再壓平」。資料若呈捲筒狀，硬壓平會把相距很遠的兩端疊在一起。流形學習（manifold learning）假設高維資料其實躺在一個低維曲面上，就像一張紙被揉進三維空間，紙本身仍是二維的。

- **多維尺度法（multidimensional scaling, MDS）**：只看兩兩點的距離，找一個讓距離盡量被保留的低維配置，因此天生不受旋轉、平移影響。
- **等距特徵映射（Isomap）**：捲筒上穿過空隙的直線距離是假的，真正的距離要沿曲面走。它把每點連到最近的幾個鄰居成圖，用圖上最短路徑當作測地距離（geodesic distance）餵給 MDS，等於把捲筒攤平。
- **局部線性嵌入（locally linear embedding, LLE）**：不管全域距離，只要求「每點都能被鄰居以固定權重線性組合出來」在低維仍成立——曲面在夠小的尺度上都近似平面。

**t-SNE（t-distributed stochastic neighbor embedding）**更極端：它把距離轉成「兩點是鄰居的機率」，再於二維平面重排點位，讓低維的鄰居機率分布貼近高維的。它保留局部鄰近結構，代價是團塊間的距離與大小不具意義。

### 收束：什麼時候降維、該選哪一種

先問「要拿降維結果做什麼」：

- **要餵給後續模型、沒有標籤**→ PCA。它產出可存下來、可重複套用的投影矩陣，能放進 pipeline。
- **要餵給分類模型、已有標籤**→ LDA。壓得比 PCA 狠（上限 c−1 維），但留下的正是分類要的資訊。
- **資料明顯是彎的、線性投影怎麼壓都不好**→ Isomap / LLE，但對鄰居數與雜訊敏感，同樣無法套用到新資料。
- **只是想畫出來看有沒有分群**→ t-SNE，且僅止於視覺化。

t-SNE 不能當前處理的理由很具體：它沒有學出一個「高維→低維」的函數，而是直接對「這一批點」做位置最佳化，所以只有 `fit_transform`、沒有 `transform`。新資料無法投影到同一張圖上，只能連同舊資料重跑，而重跑的座標跟上一次對不起來。一個無法重現、無法套用到未見資料的轉換，放進訓練／測試流程等於讓測試資料參與了擬合。標準做法是：**用 PCA 或 LDA 進模型，用 t-SNE 進眼睛**；常見折衷是先用 PCA 壓到 50 維，再餵給 t-SNE 畫圖。

> 延伸閱讀：[《Machine Learning with PyTorch and Scikit-Learn》ch05 程式碼與章節大綱](https://github.com/rasbt/machine-learning-book/tree/main/ch05)、[《Python Data Science Handbook》"In Depth: Principal Component Analysis"](https://jakevdp.github.io/PythonDataScienceHandbook/05.09-principal-component-analysis.html)（上文為獨立整理與舉例，非該書內容改寫）

{/* @ai-visualize
id: ml-week13-three-objectives
type: motion
prompt: |
  一個「同一團散點，三種降維方法在最大化什麼」的切換元件。散點資料固定不變，
  切換 PCA / LDA / t-SNE 時投影方式與標註整組改變。

  散點資料：自行生成一團二維散點約 40 個，分成兩類（用不同顏色與形狀標記），
  兩類略有重疊但整體有一個明顯的最長軸方向，且該最長軸方向刻意「不等於」
  最能分開兩類的方向——這個錯開正是 PCA 與 LDA 會選出不同方向的原因。
  座標寫死在元件裡即可。

  PCA 模式：
  - 畫一條穿過點雲最長軸的箭頭
  - 用虛線把每個點投影到該軸上，顯示投影落點散得最開
  - 標註：最大化總變異；不看標籤（unsupervised）

  LDA 模式：
  - 畫一條與 PCA 明顯不同角度的箭頭，指向最能分開兩類的方向
  - 用兩個小括號分別標示類內散布 S_W 與類間距離 S_B
  - 標註：最大化 類間距離 ÷ 類內離散；用標籤（supervised），最多降到 c−1 維

  t-SNE 模式：
  - 不畫直線軸，改畫鄰居連線把鄰近點連成小團
  - 旁邊畫出投影後的二維團塊配置（兩到三個分開的團）
  - 標註：保留局部鄰近結構；團塊之間的距離無意義

  互動規則：三選一切換。切換時箭頭以 motion 旋轉到新角度（PCA ↔ LDA 之間），
  切到 t-SNE 時箭頭淡出、鄰居連線淡入。散點本身位置絕對不動，強化「同一批資料」。

  視覺要求：
  - 手寫 SVG，viewBox 寬度不超過 660，preserveAspectRatio="xMidYMid meet"
  - 一次只顯示一種模式，用滿整個欄寬——不要做成三格並排
    （內文欄只有 647px，分成三格後每格僅約 200px，散點會糊成一團）
  - 中文標註一律用 DOM 放在 SVG 下方，不要塞進 SVG
  - 三種方法用三種色票
  - 尊重 prefers-reduced-motion

  核心洞察：三者的差別不在「壓到幾維」，而在目標函數在最大化什麼。
caption: 同一團散點，三種降維方法各自選出不同的方向：PCA 追變異、LDA 追可分性、t-SNE 只保鄰居。
status: generated
*/}

<GeneratedFrame id="ml-week13-three-objectives" type="motion" prompt={"切換 PCA / LDA / t-SNE，同一批散點用三種方式投影，看各方法的優化目標有何不同。"} caption={"同一團散點，三種降維方法各自選出不同的方向：PCA 追變異、LDA 追可分性、t-SNE 只保鄰居。"}>
  <MlWeek13ThreeObjectives client:visible />
</GeneratedFrame>

{/* @ai-visualize
id: ml-week13-selection
type: diagram
prompt: |
  一張「要拿降維結果做什麼」的決策分支圖，四個終點，節點總數控制在 8 個以內。

  從起點「要拿降維結果做什麼？」出發：
  1. 「餵給後續模型」→ 再問「有標籤嗎？」
     - 有 → LDA（註明：上限 c−1 維，c 是類別數）
     - 沒有 → PCA（註明：有 fit / transform，能放進 pipeline 當前處理）
  2. 「只是要畫圖看分群」→ t-SNE
     （註明：只有 fit_transform，無法套用到新資料，所以不能當前處理）
  3. 「資料是彎的、線性壓不好」→ 流形學習 Isomap / LLE

  視覺要求：
  - 手寫 SVG，viewBox 寬度不超過 660，preserveAspectRatio="xMidYMid meet"
  - 每個分支上標註判斷依據
  - 「t-SNE 不能當前處理」這個限制用警示色標記——這是最容易踩到的坑
  - 中文標籤估寬時每個全形字抓 1em（字級 14px 就抓 14px 寬）；
    放不下時縮短文字，不要縮字級到 10px 以下
  - 節點若一列排不下就改直向或蛇形排列

  核心洞察：能不能放進 pipeline，取決於這個方法有沒有 transform——t-SNE 沒有。
caption: 降維方法怎麼挑：先問拿來做什麼、有沒有標籤，再決定 PCA、LDA、t-SNE 還是流形學習。
status: generated
*/}

<GeneratedFrame id="ml-week13-selection" type="diagram" prompt={"從「降維用在哪裡」和「有沒有標籤」出發，四個終點決定 PCA、LDA、t-SNE 還是流形學習。"} caption={"降維方法怎麼挑：先問拿來做什麼、有沒有標籤，再決定 PCA、LDA、t-SNE 還是流形學習。"}>
  <MlWeek13Selection client:visible />
</GeneratedFrame>

{/* @ai-visualize
id: ml-week13-explained-variance
type: chart
prompt: |
  一張累積解釋變異（cumulative explained variance）圖，說明「保留幾個主成分」
  怎麼決定。

  資料（自行編寫的一組遞減比例，共 10 個主成分）：
  個別解釋變異：0.32, 0.21, 0.14, 0.10, 0.07, 0.05, 0.04, 0.03, 0.025, 0.015
  累積解釋變異：0.32, 0.53, 0.67, 0.77, 0.84, 0.89, 0.93, 0.96, 0.985, 1.00

  呈現方式：
  - 長條顯示每個主成分的「個別」解釋變異
  - 疊上一條階梯線顯示「累積」解釋變異
  - 畫兩條水平參考線：90% 與 95%
  - 標出累積首次達到 90% 的位置（第 7 個主成分）與 95% 的位置（第 8 個）

  視覺要求：
  - 用 recharts（這是有軸的量化資料，符合決策樹的選型）
  - 兩條參考線用 ReferenceLine，標籤放在圖內
  - 圖表容器寬度 100%，高度固定約 280px；
    禁止使用 sm: / md: 等 viewport breakpoint（內文欄實際只有 647px）
  - 座標軸標籤：橫軸「主成分」，縱軸「解釋變異比例」
  - 橫軸刻度用 1–10 的數字，避免中文標籤在窄圖上重疊

  核心洞察：不用先決定要留幾維，看累積解釋變異在哪裡達到 90–95% 再決定。
caption: 累積解釋變異決定保留幾個主成分：這組資料到第 7 個成分就跨過 90%，第 8 個跨過 95%。
status: generated
*/}

<GeneratedFrame id="ml-week13-explained-variance" type="chart" prompt={"長條 + 階梯線呈現每個主成分的個別與累積解釋變異，標出達到 90% / 95% 的位置。"} caption={"累積解釋變異決定保留幾個主成分：這組資料到第 7 個成分就跨過 90%，第 8 個跨過 95%。"}>
  <MlWeek13ExplainedVariance client:visible />
</GeneratedFrame>

## 教材預習重點

> 摘自 `machine-learning-book` 專案的 `STUDY_GUIDE_zh-TW.md`，對應章節：ch05。上課後請依實際授課內容調整，真實體悟請填在下方「實作心得」。

**在做什麼**：比較三種把高維資料壓縮成能看懂的維度的方法——PCA 找資料本身變異量最大的方向（unsupervised）；LDA 利用分類標籤找出最能區分類別的方向（supervised）；t-SNE 專門用來畫圖視覺化，不用來做前處理。

**關鍵概念**：PCA（explained variance）、LDA、t-SNE、feature transformation

**PDSH 對照**：`PythonDataScienceHandbook` 的 `05.09`（PCA，含 noise filtering、eigenfaces 應用）、`05.10`（Manifold Learning：multidimensional scaling、Isomap、LLE）比 ch05 對非線性降維講得更完整，適合深入非線性降維時參考。

## 實作心得

**做了什麼**：

**關鍵洞察**：

**踩了什麼坑**：

**跟預期或教材的落差**：
