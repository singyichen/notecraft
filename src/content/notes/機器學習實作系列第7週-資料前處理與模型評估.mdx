---
title: "機器學習實作系列：第 7 週 - 資料前處理、特徵工程與模型選擇評估"
description: ""
tags: ["機器學習實作系列"]
createdAt: "2026-10-19"
updatedAt: "2026-09-25"
---

import PdfRefChip from '@/components/islands/PdfRefChip.tsx'
import GeneratedFrame from '@/components/GeneratedFrame.astro'
import MlWeek7LeakageContrast from '@/components/generated/ml-week7-leakage-contrast'
import MlWeek7PipelineInFold from '@/components/generated/ml-week7-pipeline-in-fold'
import MlWeek7ConfusionMetrics from '@/components/generated/ml-week7-confusion-metrics'
import MlWeek7SearchStrategies from '@/components/generated/ml-week7-search-strategies'

# 第 7 週：資料前處理、特徵工程與模型選擇評估

*Data Preprocessing and Feature Engineering. Model Selection and Evaluation*

> 本週參考：[《Machine Learning with PyTorch and Scikit-Learn》](/notes/machine-learning-with-pytorch-and-scikit-learn)

## 本週摘要

### 髒資料的第一關：缺值與類別欄位

真實資料是一張到處有洞、還混著文字的試算表。**缺值（missing data）** 只有刪或補兩條路：刪整列很乾脆，但某欄十筆有八筆是空的時，該刪的是整欄；補值拿平均數或中位數填，等於說「不知道就當它跟大家一樣」，快，卻把該欄的變異壓平了。類別欄位則要分有序與無序：尺寸 S / M / L 映射成 1 / 2 / 3 合理，顏色紅 / 綠 / 藍若編成 0 / 1 / 2，模型會把「藍是綠的兩倍」當真，得改用**獨熱編碼（one-hot encoding）** 拆成數個 0/1 欄位——代價是欄位膨脹且彼此線性相關，常丟掉一欄避免共線性。

### 先切開，再縮放

模型在訓練資料上考幾分不算數，要留一份它沒看過的當期末考——**訓練／測試切分（train/test split）** 通常留兩到三成，分類問題還要分層（stratify）維持類別比例。切完才縮放：坪數是幾十、總價是幾百萬，靠距離或權重運作的演算法（KNN、SVM、帶正則化的線性模型）會被數值大的欄位蓋過去。關鍵是平均與標準差**只能從訓練集算**再套到測試集——測試集的統計量一旦參與計算，答案就偷跑了。

### 特徵太多時的減法

欄位越多，模型越容易連雜訊都背起來。**正則化（regularization）** 在損失函數加一項對大權重的懲罰：$L_2$ 罰平方和，把權重一起往零壓卻很少壓成零；$L_1$ 罰絕對值和，約束區域帶尖角，最佳解常落在座標軸上，一部分權重成為真正的 0，等於順手做了特徵選擇（強度由 `C` 控制，它是懲罰力道的倒數）。另一條路是隨機森林的**特徵重要度（feature importance）**：看某欄在多少分裂點上降低了不純度——但相關的兩欄會互相稀釋分數，低分不代表沒資訊。

### 樞紐：資料洩漏與 pipeline

前兩節藏著同一個陷阱。**資料洩漏（data leakage）** 是評估時模型間接看到了不該看的資料，最典型的就是先對整份資料補缺值、標準化再切訓練與測試——補值的平均數混進了測試集，分數漂亮，上線就掉。**管線（pipeline）** 為此而生：把補值、編碼、縮放、模型串成單一物件，fit 時每段轉換器只用當下的訓練資料 fit，predict 時只做 transform。

這在 **$k$ 折交叉驗證（k-fold cross-validation）** 裡才真正致命：把訓練資料切 $k$ 份輪流當驗證、跑 $k$ 輪取平均與標準差，若前處理是在切 fold 之前做的，每輪驗證集都被污染、$k$ 個分數一起偏高。把整條 pipeline 當成「模型」丟給交叉驗證，前處理才會在每個 fold 內各自 fit 一次。

### 分類模型錯在哪

準確率最會騙人：健檢資料只有 1% 陽性時，永遠答「陰性」也有 99% 準確率——這是**類別不平衡（class imbalance）**，得靠過採樣、加大少數類別的損失權重或換指標處理。拆開**混淆矩陣（confusion matrix）** 才看得細：真陽性、真陰性、偽陽性（誤報）、偽陰性（漏報）四格。**精確率（precision）** 問「我說是陽性的有幾成真的是」，**召回率（recall）** 問「所有真陽性我抓到幾成」，門檻調鬆則 recall 升、precision 降。癌症篩檢寧可誤報不能漏報，優先 recall；垃圾信過濾誤殺重要信代價高，偏向 precision。**ROC 曲線（receiver operating characteristic curve）** 則把門檻從最嚴掃到最鬆，畫出偽陽性率對真陽性率的軌跡，量的是排序能力而非單一門檻，AUC 0.5 等於瞎猜。

### 收束：看到曲線該怎麼行動

**學習曲線（learning curve）** 橫軸是訓練樣本數，訓練與驗證兩條線的形狀直接指定下一步：兩條都低且已貼合是高偏差、欠擬合，**再多資料也沒用**，該加特徵、換更複雜的模型或調弱正則化；訓練高、驗證低、中間一道持續的縫是高變異、過擬合，這時**加資料有效**，也可加強正則化或砍特徵；兩條都高且合攏，代表已吃滿這份資料，要進步得換特徵或換演算法。**驗證曲線（validation curve）** 把橫軸換成某個超參數，中間驗證分數見頂、右端訓練續升而驗證掉頭，峰值就是該選的值。超參數不只一個時用**網格搜尋（grid search）** 窮舉組合，或**隨機搜尋（randomized search）** 從分布抽固定次數——真正關鍵的參數通常只有少數幾個，同樣預算下後者更划算。

判斷模型是否真的有效，三個條件缺一不可：前處理全程在 pipeline 裡、成績看交叉驗證的平均與標準差而非單次切分、指標符合這個問題的代價結構。測試集只在最後動一次，用來確認，不用來挑選。

> 延伸閱讀：[《Machine Learning with PyTorch and Scikit-Learn》ch04、ch06 程式碼與章節大綱](https://github.com/rasbt/machine-learning-book)、[《Python Data Science Handbook》"Hyperparameters and Model Validation"](https://jakevdp.github.io/PythonDataScienceHandbook/05.03-hyperparameters-and-model-validation.html)（上文為獨立整理與舉例，非該書內容改寫）

{/* @ai-visualize
id: ml-week7-leakage-contrast
type: motion
prompt: |
  一個上下並排、可逐步播放的對比元件，凸顯資料洩漏究竟發生在哪一步。

  上排「會洩漏的做法」四個步驟：
  1. 整份原始資料
  2. 補缺值 / one-hot / 標準化 ← 這一步標警示：轉換器的統計量已經看過全部資料
  3. 才切 train / test 與 k 個 fold
  4. 交叉驗證分數虛高

  下排「正確做法」四個步驟：
  1. 原始資料
  2. 先切出 test，留到最後才動
  3. 訓練集再切成 k 個 fold，每個 fold 內各自跑一次 pipeline
  4. k 個分數取平均與標準差，最後才用一次 test 確認

  互動規則：一個「下一步」按鈕加 1–4 的步驟指示器，上下兩排同步推進到同一步，
  讓讀者看到兩條路在第 2、3 步就分岔了。推進到上排第 2 步時，警示標記以 motion 淡入。

  視覺要求：
  - 上下兩排各自是一列四個節點，用 flex-wrap，節點過窄時自動換行；
    禁止使用 sm: / md: 等 viewport breakpoint（內文欄實際只有 647px）
  - 上排用警示色系，下排用正常色系
  - 第 4 步右側各標一個分數示意：上排「0.95（虛高）」、下排「0.87 ± 0.03（誠實估計）」
  - 警示圖示用 lucide-react 的 TriangleAlert，不要用 emoji
  - 尊重 prefers-reduced-motion，關閉動畫時四步一次顯示

  核心洞察：洩漏不是模型的問題，是「轉換器在切分之前就看過測試資料」造成的。
caption: 洩漏發生在哪一步：把前處理放在切分之前，分數會虛高；先切再處理，才是誠實的估計。
status: generated
*/}

<GeneratedFrame id="ml-week7-leakage-contrast" type="motion" prompt={"對比會洩漏與正確做法，逐步展示資料洩漏發生在前處理的哪一步。"} caption={"洩漏發生在哪一步：把前處理放在切分之前，分數會虛高；先切再處理，才是誠實的估計。"}>
  <MlWeek7LeakageContrast client:visible />
</GeneratedFrame>

{/* @ai-visualize
id: ml-week7-pipeline-in-fold
type: motion
prompt: |
  放大單一 fold 的內部，說明 pipeline 裡的 fit 與 transform 分別發生在哪。

  版面（上下兩區）：
  上區是 k = 5 的 fold 條狀圖——五條橫帶，當前 fold 的驗證段用強調色，其餘四段是訓練段。
  下區是被放大的當前 fold 內部。

  下區的 pipeline 容器，框住四個依序串接的階段：
  1. 補缺值 SimpleImputer
  2. one-hot OneHotEncoder
  3. 標準化 StandardScaler
  4. 模型 LogisticRegression

  每個階段標兩件事：
  - 對訓練段：fit + transform（用實線箭頭）
  - 對驗證段：只有 transform（用虛線箭頭，並標「不 fit」）

  互動規則：讀者可切換當前是第幾個 fold（1–5），上區條狀圖的強調段跟著移動，
  下區的訓練段 / 驗證段標籤同步更新。切換時強調段以 motion 平移。

  視覺要求：
  - fold 條狀圖用手寫 SVG，viewBox 寬度不超過 660，preserveAspectRatio="xMidYMid meet"
  - pipeline 容器用 DOM（不要畫進 SVG），四個階段用 flex-wrap
  - 實線與虛線箭頭的差別要明顯，並附一個小圖例：
    實線 = fit + transform，虛線 = 只有 transform
  - 尊重 prefers-reduced-motion

  核心洞察：pipeline 的價值不是少寫幾行，而是讓「fit 只能看訓練段」
  變成結構上做不到違反。
caption: 放大一個 fold 看 pipeline：訓練段才 fit，驗證段只有 transform——這個限制是被結構保證的。
status: generated
*/}

<GeneratedFrame id="ml-week7-pipeline-in-fold" type="motion" prompt={"放大單一 fold，展示 pipeline 中轉換器對訓練段 fit、對驗證段只 transform 的差別。"} caption={"放大一個 fold 看 pipeline：訓練段才 fit，驗證段只有 transform——這個限制是被結構保證的。"}>
  <MlWeek7PipelineInFold client:visible />
</GeneratedFrame>

## 模型選擇：兩個層次

> 本節起對應課堂講義 `第7週-特徵工程與模型選擇評估.pdf`（Lecture 6）。講義開場定義：模型選擇就是「在所有可行的方案中，找出對特定資料集泛化能力最好、預測誤差最小的模型」。

{/* @ai-reference
id: model-selection-definition
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 1
status: suggested
excerpt: 在所有可行的方案中，找出對特定資料集泛化能力最好、預測誤差最小的模型
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={1} status="suggested" excerpt={"在所有可行的方案中，找出對特定資料集泛化能力最好、預測誤差最小的模型"} client:visible />

「模型選擇」這個詞在講義裡有寬窄兩義，分清楚才不會誤會該用哪套方法：

{/* @ai-reference
id: model-selection-scope
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 4
status: suggested
excerpt: Hyperparameter Tuning (in the narrow sense)：Select the optimal values of tuning parameters；Algorithm Selection (in the broad sense)：Select the best model/algorithm
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={4} status="suggested" excerpt={"Hyperparameter Tuning (in the narrow sense)：Select the optimal values of tuning parameters；Algorithm Selection (in the broad sense)：Select the best model/algorithm"} client:visible />

| 層次 | 中文 | 在選什麼 |
| --- | --- | --- |
| Hyperparameter tuning（狹義） | 超參數調整 | 對**指定的**模型／演算法，選出調校參數的最佳值 |
| Algorithm selection（廣義） | 演算法選擇 | 比較不同模型／演算法各自的最佳表現，選出最好的那個 |

兩者的差別在後面會直接影響驗證架構：狹義的調參用單層交叉驗證就夠，廣義的演算法比較則需要巢狀交叉驗證。講義的建議很直接：至少比較數個不同的機器學習演算法，再決定用哪一個。

{/* @ai-reference
id: model-selection-compare-count
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 5
status: suggested
excerpt: Compare at least a handful of different ML algorithms to select the best model
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={5} status="suggested" excerpt={"Compare at least a handful of different ML algorithms to select the best model"} client:visible />

## 交叉驗證：從 Holdout 到 k 折

### Holdout 交叉驗證與它的弱點

**保留法交叉驗證（holdout cross-validation）** 把資料切成三份，各司其職：

| 資料集 | 用途 |
| --- | --- |
| 訓練集 | 模型擬合 |
| 驗證集 | 模型評估、模型選擇 |
| 測試集 | 模型評估、泛化效能估計 |

{/* @ai-reference
id: holdout-cv-basics
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 7
status: suggested
excerpt: Training dataset：Model fitting；Validation dataset：Model evaluation, Model selection；Test dataset：Generalization performance estimate。Disadvantage：Sensitive to how we partition the training dataset
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={7} status="suggested" excerpt={"Training dataset：Model fitting；Validation dataset：Model evaluation, Model selection；Test dataset：Generalization performance estimate。Disadvantage：Sensitive to how we partition the training dataset"} client:visible />

這裡的關鍵是驗證集與測試集**不能是同一份**：驗證集參與了選擇，已經不再「未見過」，拿它報告泛化效能會偏樂觀。

缺點也很明顯：結果**對「訓練集與驗證集怎麼分」很敏感**——換一種切法，結論可能就變了。

### k 折交叉驗證

**$k$ 折交叉驗證（k-fold cross-validation）** 解決的正是這件事。把訓練資料隨機不放回地切成 $k$ 折，每輪取 $k-1$ 折訓練、剩下 1 折評估，重複 $k$ 次得到 $k$ 個模型與 $k$ 個效能估計。取平均後，對子分割方式的敏感度就大幅下降。

{/* @ai-reference
id: kfold-cv-basics
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 8
status: suggested
excerpt: Average performance of k models is less sensitive to the sub-partitioning of training data
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={8} status="suggested" excerpt={"Average performance of k models is less sensitive to the sub-partitioning of training data"} client:visible />

$k$ 的選法有個簡單的反向關係：

| 資料量 | $k$ | 理由 |
| --- | --- | --- |
| 小 | 大 | 每折留下的訓練資料才夠多 |
| 大 | 小 | 省計算成本，估計本來就夠穩 |

$k = 10$ 是常用的標準值。極端情況是 $k$ 等於訓練樣本數，稱為**留一交叉驗證（leave-one-out cross-validation）** 。

{/* @ai-reference
id: kfold-cv-k-choice
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 9
status: suggested
excerpt: k = 10 (a good standard value)；Small dataset → Large k；Large dataset → Small k；leave-one-out cross-validation 為 k 等於訓練樣本數的特例
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={9} status="suggested" excerpt={"k = 10 (a good standard value)；Small dataset → Large k；Large dataset → Small k；leave-one-out cross-validation 為 k 等於訓練樣本數的特例"} client:visible />

兩個容易忽略的細節：

- 找到滿意的超參數後，要**用完整的訓練集重新訓練一次**。我們要的是單一最終模型，而不是那 $k$ 個個別模型；訓練樣本越多，模型通常越準也越穩健。
- **分層 $k$ 折交叉驗證（stratified k-fold cross-validation）** 會在每一折中保持類別標籤的比例，分類問題應優先使用。

{/* @ai-reference
id: kfold-cv-details
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 10
status: suggested
excerpt: Once found satisfactory hyperparameter values, retrain the model on the complete training dataset；Stratified k-fold：Class label proportions are preserved in each fold
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={10} status="suggested" excerpt={"Once found satisfactory hyperparameter values, retrain the model on the complete training dataset；Stratified k-fold：Class label proportions are preserved in each fold"} client:visible />

## 診斷曲線：學習曲線與驗證曲線

兩者的差別只在橫軸，但回答的問題不同：

| 曲線 | 橫軸 | 回答什麼 |
| --- | --- | --- |
| **學習曲線（learning curve）** | 訓練資料量 | 是高變異還是高偏差？**再蒐集更多資料有沒有用**？ |
| **驗證曲線（validation curve）** | 某個超參數的值 | 是高變異還是高偏差？這個超參數該選多少？ |

學習曲線的形狀直接對應處方：

| 症狀 | 圖上長相 | 對策 |
| --- | --- | --- |
| 高偏差（欠擬合） | 訓練與交叉驗證的正確率**都低** | 增加模型參數數量、降低正則化強度 |
| 高變異（過擬合） | 訓練與交叉驗證正確率之間**有明顯的縫** | 蒐集更多訓練資料、降低模型複雜度、提高正則化參數、減少特徵數 |

{/* @ai-reference
id: learning-curve-diagnosis
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 12
status: suggested
excerpt: High bias：Both low training and cross-validation accuracy；High variance：Large gap between training and cross-validation accuracy，及各自的 solutions
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={12} status="suggested" excerpt={"High bias：Both low training and cross-validation accuracy；High variance：Large gap between training and cross-validation accuracy，及各自的 solutions"} client:visible />

這張圖最實用的地方在於它能回答「該不該再去要資料」。高偏差時兩條線已經貼合且都低，再多資料也只是把同一條平線延長；高變異時兩條線之間有持續的縫，補資料才可能把縫拉近。

講義也誠實標出這條經驗法則的邊界：蒐集更多訓練資料通常會降低過擬合的機會，但**並非總是有效**——例如訓練資料雜訊極大，或模型已經非常接近最佳。

{/* @ai-reference
id: learning-curve-caveat
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 11
status: suggested
excerpt: While collecting more training data usually tends to decrease the chance of overfitting, it may not always help, for example, if the training data is extremely noisy or the model is already very close to optimal
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={11} status="suggested" excerpt={"While collecting more training data usually tends to decrease the chance of overfitting, it may not always help, for example, if the training data is extremely noisy or the model is already very close to optimal"} client:visible />

## 超參數搜尋的三種策略

找到（次）最佳的超參數組合，靠的是「交叉驗證＋搜尋策略」。講義列了三種：

{/* @ai-reference
id: hp-search-strategy-intro
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 16
status: suggested
excerpt: Cross-validation + search strategy：Grid search／Randomized search／Successive halving search
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={16} status="suggested" excerpt={"Cross-validation + search strategy：Grid search／Randomized search／Successive halving search"} client:visible />

| 策略 | 中文 | 做法 | 取捨 |
| --- | --- | --- | --- |
| Grid search | 格狀搜尋 | 為每個超參數指定候選值清單，窮舉所有組合構成網格，逐一用交叉驗證評估 | 只要最佳組合在網格內就**保證找得到**；網格一大，成本急遽上升 |
| Randomized search | 隨機搜尋 | 從分布或離散集合中隨機抽取超參數組合 | 用更低的成本與時間探索**更廣**的取值範圍 |
| Successive halving | 逐次減半搜尋 | 先用少量資源評估所有候選，反覆淘汰表現差的、把資源集中到存活者，直到只剩一組 | 資源效率最高；講義的白話註解是「練蠱」 |

{/* @ai-reference
id: hp-search-grid
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 17
status: suggested
excerpt: Guaranteed to find the optimal hyperparameter configuration if it is contained in the specified grid；Specifying large hyperparameter grids makes grid search very expensive in practice
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={17} status="suggested" excerpt={"Guaranteed to find the optimal hyperparameter configuration if it is contained in the specified grid；Specifying large hyperparameter grids makes grid search very expensive in practice"} client:visible />

{/* @ai-reference
id: hp-search-random
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 18
status: suggested
excerpt: Explore a wider range of hyperparameter value settings in a more cost- and time-effective manner
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={18} status="suggested" excerpt={"Explore a wider range of hyperparameter value settings in a more cost- and time-effective manner"} client:visible />

{/* @ai-reference
id: hp-search-halving
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 19
status: suggested
excerpt: The following steps are repeated until only one hyperparameter configuration remains；More resource-efficient hyperparameter search
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={19} status="suggested" excerpt={"The following steps are repeated until only one hyperparameter configuration remains；More resource-efficient hyperparameter search"} client:visible />

{/* @ai-visualize
id: ml-week7-search-strategies
type: motion
prompt: |
  比較三種超參數搜尋策略在同一個二維超參數空間裡的行為差異。

  共用設定：一個二維搜尋空間，橫軸是 C（對數尺度，例如 0.001 到 1000），
  縱軸是 gamma（對數尺度，同範圍）。背景用很淡的等高線暗示「真正的最佳區域」
  落在偏某一角的一小塊，且**只有 C 這個維度真的重要**（等高線沿 gamma 方向幾乎平坦）。

  三種策略各一個面板，可用分頁或並排切換：
  1. 格狀搜尋（grid search）—— 規則的網格點，例如 5×5 = 25 次評估，
     點整齊排列；標註「保證找到網格內的最佳解，但在不重要的維度上浪費了大量評估」
  2. 隨機搜尋（randomized search）—— 25 個散落的點（位置寫死，不要隨機生成），
     在 C 這個維度上試到的相異值遠多於格狀搜尋的 5 個；
     標註「同樣 25 次，在真正重要的維度上取樣密度高得多」
  3. 逐次減半搜尋（successive halving）—— 分三輪顯示：
     第一輪很多候選、每個只用少量資源；第二輪淘汰一半、資源加倍；第三輪剩少數、資源再加倍。
     用點的大小表示投入的資源量，被淘汰的點淡出

  互動規則：切換三個策略時，點以 motion 重新排布。
  逐次減半那一頁另有「下一輪」按鈕，逐輪推進並顯示當前候選數與每個候選的資源量。
  每個面板下方固定顯示「總評估成本」的相對量，讓三者可以比較。

  視覺要求：
  - 搜尋空間用手寫 SVG，viewBox 寬度不超過 660，preserveAspectRatio="xMidYMid meet"
  - 兩軸標 log 刻度的代表值即可，不需要精確刻度
  - 中文說明與成本數字放在 SVG 外的 DOM
  - 最佳區域用強調色的淡填色，候選點用中性色，命中最佳區域的點用強調色
  - 面板固定高度，避免切換時版面跳動
  - 尊重 prefers-reduced-motion，關閉動畫時直接顯示最終排布

  核心洞察：格狀搜尋的浪費不在「試得不夠多」，而在它被迫在每個維度上都試同樣多次——
  可是真正重要的超參數通常只有少數幾個。隨機搜尋用同樣的預算，在重要維度上取樣得更密；
  逐次減半則再進一步，把資源從一開始就不平均地分配給表現好的候選。
caption: 同樣 25 次評估，格狀搜尋在不重要的維度上重複浪費，隨機搜尋把預算花在真正重要的那一軸上。
status: generated
*/}

<GeneratedFrame id="ml-week7-search-strategies" type="motion" prompt={"比較三種超參數搜尋策略在同一個二維超參數空間裡的行為差異。"} caption={"同樣 25 次評估，格狀搜尋在不重要的維度上重複浪費，隨機搜尋把預算花在真正重要的那一軸上。"}>
  <MlWeek7SearchStrategies client:visible />
</GeneratedFrame>

## 巢狀交叉驗證：演算法選擇的正確做法

要比較「不同演算法各自調到最好之後」誰比較強，單層交叉驗證是不夠的——超參數是在同一份資料上挑的，分數會樂觀。**巢狀交叉驗證（nested cross-validation）** 用兩層迴圈解決：

{/* @ai-reference
id: nested-cv-basics
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 20
status: suggested
excerpt: Outer k-fold cross-validation loop to split the data into training and test folds；Inner loop is used to select the model；the test fold is then used to evaluate the model performance
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={20} status="suggested" excerpt={"Outer k-fold cross-validation loop to split the data into training and test folds；Inner loop is used to select the model；the test fold is then used to evaluate the model performance"} client:visible />

- **外層** $k$ 折把資料切成訓練折與測試折
- **內層**在外層的訓練折上再跑一次 $k$ 折交叉驗證，用來選模型（挑超參數）
- 選完之後，用外層的測試折評估該模型的效能

這樣得到的，才是「我們調完超參數、拿去面對沒看過的資料」時可以期待的合理估計。講義的範例是 **$5 \times 2$ 交叉驗證**——外層 5 折、內層 2 折，最後取外層 5 個分數的平均作為演算法選擇的依據。

{/* @ai-reference
id: nested-cv-5x2-example
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 21
status: suggested
excerpt: Example：5 × 2 cross-validation；Model selection；Average performance used for algorithm selection
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={21} status="suggested" excerpt={"Example：5 × 2 cross-validation；Model selection；Average performance used for algorithm selection"} client:visible />

## 模型評估：混淆矩陣與效能指標

### 混淆矩陣

**混淆矩陣（confusion matrix）** 是一個方陣，記錄分類器各種預測的計數：真陽性（TP）、真陰性（TN）、偽陽性（FP）、偽陰性（FN）。本節所有指標都從這四個數字長出來——把它們當成八個要背的公式，不如記住它們是同四格數字的八種讀法。

{/* @ai-reference
id: confusion-matrix-def
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 23
status: suggested
excerpt: A square matrix that reports the counts of predictions of a classifier：True Positive (TP)／True Negative (TN)／False Positive (FP)／False Negative (FN)
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={23} status="suggested" excerpt={"A square matrix that reports the counts of predictions of a classifier：True Positive (TP)／True Negative (TN)／False Positive (FP)／False Negative (FN)"} client:visible />

### 錯誤率與正確率

$$
\begin{aligned}
\text{ERR} &= \frac{FP + FN}{FP + FN + TP + TN} \\[6pt]
\text{ACC} &= \frac{TP + TN}{FP + FN + TP + TN} = 1 - \text{ERR}
\end{aligned}
$$

{/* @ai-reference
id: err-acc-formulas
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 24
status: suggested
excerpt: Prediction Error (ERR)（錯誤率）／Accuracy (ACC)（正確率）
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={24} status="suggested" excerpt={"Prediction Error (ERR)（錯誤率）／Accuracy (ACC)（正確率）"} client:visible />

### 真陽率與偽陽率

處理**類別不平衡（class imbalance）** 時，下面這兩個比正確率有用：

{/* @ai-reference
id: tpr-fpr-formulas
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 25
status: suggested
excerpt: True Positive Rate (TPR)（真陽率）／False Positive Rate (FPR)（偽陽率）。Useful for imbalanced class problems
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={25} status="suggested" excerpt={"True Positive Rate (TPR)（真陽率）／False Positive Rate (FPR)（偽陽率）。Useful for imbalanced class problems"} client:visible />

$$
\begin{aligned}
\text{TPR} &= \frac{TP}{P} = \frac{TP}{FN + TP} \\[6pt]
\text{FPR} &= \frac{FP}{N} = \frac{FP}{FP + TN}
\end{aligned}
$$

### 精確率、召回率與 F1

**精確率（precision）** 與**召回率（recall）** 問的是不同的問題——前者問「我說是陽性的，有幾成真的是」，後者問「所有真的陽性，我抓到了幾成」：

{/* @ai-reference
id: precision-recall-formulas
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 26
status: suggested
excerpt: Precision (PRE)（精確率）／Recall (REC)（召回率）
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={26} status="suggested" excerpt={"Precision (PRE)（精確率）／Recall (REC)（召回率）"} client:visible />

$$
\begin{aligned}
\text{PRE} &= \frac{TP}{TP + FP} \\[6pt]
\text{REC} &= \text{TPR} = \frac{TP}{FN + TP}
\end{aligned}
$$

兩者要合成單一數字時用 **F1 分數**，取的是調和平均——調和平均對「兩者中較小的那個」特別敏感，所以只有兩邊都高，F1 才會高：

{/* @ai-reference
id: f1-score-formula
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 27
status: suggested
excerpt: F1 Score：Harmonic mean of PRE and REC
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={27} status="suggested" excerpt={"F1 Score：Harmonic mean of PRE and REC"} client:visible />

$$
\text{F1} = 2 \cdot \frac{\text{PRE} \times \text{REC}}{\text{PRE} + \text{REC}}
$$

### MCC

**馬修斯相關係數（Matthews correlation coefficient, MCC）** ——又稱 Phi 相關係數——同時吃四格數字，因此在類別不平衡時比 F1 更不容易被單邊表現騙過，值域是 $-1 \le \text{MCC} \le 1$：

{/* @ai-reference
id: mcc-formula
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 27
status: suggested
excerpt: Matthews Correlation Coefficient (MCC)（馬修斯相關係數 或 Phi 相關係數），−1 ≤ MCC ≤ 1
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={27} status="suggested" excerpt={"Matthews Correlation Coefficient (MCC)（馬修斯相關係數 或 Phi 相關係數），−1 ≤ MCC ≤ 1"} client:visible />

$$
\text{MCC} = \frac{TP \times TN - FP \times FN}{\sqrt{(TP + FP)(TP + FN)(TN + FP)(TN + FN)}}
$$

### ROC 曲線與 AUC

前面所有指標都綁在某一個決策門檻上。**受試者操作特徵曲線（receiver operating characteristic curve, ROC）** 換一個角度：把門檻從最嚴掃到最鬆，畫出 FPR 對 TPR 的軌跡，量的是模型的**排序能力**而非單一門檻下的表現。

{/* @ai-reference
id: roc-curve-def
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 28
status: suggested
excerpt: Receiver Operating Characteristic (ROC)：Curve of FPR v.s. TPR, computed by shifting the decision threshold；ROC AUC 範圍 [0,1]，Perfect／Random Guessing
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={28} status="suggested" excerpt={"Receiver Operating Characteristic (ROC)：Curve of FPR v.s. TPR, computed by shifting the decision threshold；ROC AUC 範圍 [0,1]，Perfect／Random Guessing"} client:visible />

曲線下面積 **ROC AUC** 落在 $[0, 1]$——完美分類器是 1，隨機猜測是 0.5，也就是對角線。

{/* @ai-visualize
id: ml-week7-confusion-metrics
type: motion
prompt: |
  一個可調門檻的混淆矩陣計算器，把「門檻移動 → 四格數字改變 → 八個指標跟著變」這條因果鏈攤開。
  這是本週所有指標公式的共同來源，做成互動比逐條背公式有效得多。

  資料：內建一組固定的模擬資料（不需真實資料集），例如 200 筆樣本，
  其中 60 筆為正類、140 筆為負類，各自給一個 0 到 1 的預測分數，
  正類的分數分布偏高、負類偏低但兩者有重疊（重疊才有得調）。分數寫死在元件裡，不要隨機生成。

  版面（由上而下三區）：
  1. 門檻滑桿（0 到 1），旁邊顯示當前門檻值
  2. 2×2 混淆矩陣，四格標 TP / FN / FP / TN 與當前計數；
     列是真實類別、欄是預測類別，並標清楚哪一軸是哪個
  3. 指標面板，同步顯示八個值：ERR、ACC、TPR、FPR、PRE、REC、F1、MCC
     每個值下方用極小的字標出它的公式（用四格的符號寫，例如 PRE = TP/(TP+FP)）

  互動規則：拖動門檻滑桿時，四格計數與八個指標即時更新，變動的數字以 motion 做短暫的顏色脈衝。
  門檻調低時 REC 升、PRE 降；調高時相反——這個反向關係要能被讀者親手試出來。
  另外放兩個預設按鈕：「偏重召回率（篩檢情境）」與「偏重精確率（垃圾信情境）」，
  點了把門檻移到對應位置並以 motion 平滑過渡。

  視覺要求：
  - 混淆矩陣用 DOM 表格或 grid，不要做成 SVG
  - 指標面板用 grid-cols-4 固定值（8 個值排成兩列），
    禁止使用 sm: / md: 等 viewport breakpoint（內文欄實際只有 647px）
  - TP / TN 用正向色，FP / FN 用警示色，色彩一律取自 notecraft-design 的 token
  - 面板固定高度，避免拖動滑桿時版面跳動
  - 公式用行內小字，不需要 KaTeX，用等寬字體 var(--font-mono) 即可
  - 尊重 prefers-reduced-motion，關閉動畫時只更新數字不做脈衝

  核心洞察：八個指標不是八個要背的公式，而是同四個數字的八種讀法。
  門檻一動，四個數字就重新分配——精確率與召回率的拉鋸就是從這裡來的。
caption: 八個指標其實是同四格數字的八種讀法：拖動門檻，親手看精確率與召回率如何此消彼長。
status: generated
*/}

<GeneratedFrame id="ml-week7-confusion-metrics" type="motion" prompt={"一個可調門檻的混淆矩陣計算器，把「門檻移動 → 四格數字改變 → 八個指標跟著變」這條因果鏈攤開。"} caption={"八個指標其實是同四格數字的八種讀法：拖動門檻，親手看精確率與召回率如何此消彼長。"}>
  <MlWeek7ConfusionMetrics client:visible />
</GeneratedFrame>

## 多類別與類別不平衡

### 微平均與宏平均

$k$ 類（$k > 2$）問題要把二元指標推廣，有兩種加權方式：

{/* @ai-reference
id: micro-macro-average
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 29
status: suggested
excerpt: Micro-average（微平均）：Weight each instance equally，關注每一個樣本；Macro-average（宏平均）：Weight all classes equally，關注每一個類別
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={29} status="suggested" excerpt={"Micro-average（微平均）：Weight each instance equally，關注每一個樣本；Macro-average（宏平均）：Weight all classes equally，關注每一個類別"} client:visible />

| 平均方式 | 中文 | 權重給誰 | 特性 |
| --- | --- | --- | --- |
| Micro-average | 微平均 | 每一個樣本（或預測）等權 | 關注「每一個樣本」，由樣本數多的類別主導 |
| Macro-average | 宏平均 | 每一個類別等權 | 關注「每一個類別」，所有類別一視同仁，**適合類別不平衡** |

補充一個容易混淆的點：在 `classification_report` 裡，micro avg 等同於正確率，而且各指標的值會相同（因為總 FP 等於總 FN）。

{/* @ai-reference
id: micro-avg-classification-report
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 46
status: suggested
excerpt: Micro avg corresponds to accuracy and would be the same for all metrics (total FP = total FN)
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={46} status="suggested" excerpt={"Micro avg corresponds to accuracy and would be the same for all metrics (total FP = total FN)"} client:visible />

### 類別不平衡的三種處理

當一個或多個類別（多數類）在資料集中被過度代表時：

{/* @ai-reference
id: class-imbalance-strategies
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 30
status: suggested
excerpt: Focus on other metrics (e.g., precision or recall) than accuracy；Upsample the minority class, downsample the majority class, and generate synthetic training examples；setting class_weight='balanced'
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={30} status="suggested" excerpt={"Focus on other metrics (e.g., precision or recall) than accuracy；Upsample the minority class, downsample the majority class, and generate synthetic training examples；setting class_weight='balanced'"} client:visible />

1. **改看別的指標**——精確率或召回率，而不是正確率
2. **動資料**——對少數類過採樣、對多數類欠採樣，或生成合成的訓練樣本
3. **動損失**——對少數類的錯誤預測給予更大的懲罰，在 scikit-learn 分類器中設定 `class_weight='balanced'`

第三種的機制是：模型猜錯一個小類樣本時，所受的損失會乘上該權重，使其影響力等同於多個大類樣本。講義也提到 `imbalanced-learn` 是專門處理重採樣技術的函式庫。

{/* @ai-reference
id: imbalance-mechanism-detail
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 30
status: suggested
excerpt: 當模型猜錯一個「小類」的樣本時，它所受到的懲罰 (Loss) 會乘以該權重，使其影響力等同於多個大類樣本；imbalanced-learn is a library focused on re-sampling techniques
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={30} status="suggested" excerpt={"當模型猜錯一個「小類」的樣本時，它所受到的懲罰 (Loss) 會乘以該權重，使其影響力等同於多個大類樣本；imbalanced-learn is a library focused on re-sampling techniques"} client:visible />

## Scikit-learn 對應 API 與實作

### model_selection 與 metrics

| 模組 | 類別／函數 | 用途 |
| --- | --- | --- |
| `model_selection` | `StratifiedKFold` | 分層 $k$ 折，提供切分用的索引；`shuffle` 預設為 `False` |
| `model_selection` | `cross_val_score()` | 直接用分層 $k$ 折算出分數；`n_jobs=-1` 用上所有 CPU 平行運算 |
| `model_selection` | `learning_curve()` | 對不同訓練集大小算出交叉驗證的訓練與測試分數 |
| `model_selection` | `validation_curve()` | 對不同超參數值算出訓練與測試分數 |
| `model_selection` | `GridSearchCV` | 對指定參數值做窮舉搜尋，`best_estimator_` 取回最佳模型 |
| `model_selection` | `RandomizedSearchCV` | 從分布抽樣固定次數；分布需提供 `rvs` 方法（如 `scipy.stats.distributions`） |
| `model_selection` | `HalvingRandomSearchCV` | 逐次減半的隨機搜尋 |
| `metrics` | `confusion_matrix()` | 計算混淆矩陣，可配 Matplotlib 的 `matshow()` 視覺化 |
| `metrics` | `precision_score()` 等 | 精確率、召回率、F1、`matthews_corrcoef()` |
| `metrics` | `roc_curve()`／`auc()`／`roc_auc_score()` | ROC 曲線與 AUC（`auc()` 用梯形法則） |
| `metrics` | `classification_report()` | 輸出主要分類指標的文字報告 |
| `metrics` | `make_scorer()` | 從效能指標或損失函數造出 scorer，可指定不同的正類標籤 |

兩個關鍵細節：

- **在 Pipeline 內指定估計器的參數要用雙底線 `__` 語法**（例如 `logisticregression__C`）。

{/* @ai-reference
id: pipeline-double-underscore
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 36
status: suggested
excerpt: Inside a Pipeline, an estimator's parameter can be specified using '__' syntax
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={36} status="suggested" excerpt={"Inside a Pipeline, an estimator's parameter can be specified using '__' syntax"} client:visible />

- scikit-learn 的**正類是標記為 class 1 的那一類**；`roc_auc_score` 在二元分類時要求輸入的分數代表「標籤較大的那一類」的機率。

{/* @ai-reference
id: sklearn-positive-class
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 47
status: suggested
excerpt: The positive class in scikit-learn is the class labeled as class 1
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={47} status="suggested" excerpt={"The positive class in scikit-learn is the class labeled as class 1"} client:visible />

巢狀交叉驗證在 scikit-learn 裡的寫法就是把兩者套起來：`cross_val_score(GridSearchCV(...))` ——外層是 $k$ 折，內層是 $k$ 折加格狀搜尋。

{/* @ai-reference
id: nested-cv-sklearn-code
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 41
status: suggested
excerpt: Nested cross-validation with grid search：cross_val_score(GridSearchCV)；Outer k-fold／Inner k-fold + grid search
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={41} status="suggested" excerpt={"Nested cross-validation with grid search：cross_val_score(GridSearchCV)；Outer k-fold／Inner k-fold + grid search"} client:visible />

### 實作案例：把上週的 pipeline 調到最好

延續上週的 Breast Cancer Wisconsin 資料集，這週補上缺的兩塊：用**訓練集**做交叉驗證式格狀搜尋（5 折平均）選出最佳模型，再用**測試集**評估（混淆矩陣＋分類報告），並針對個別的偽陰性（FN）逐筆檢視：

{/* @ai-reference
id: pipeline-best-model-implementation
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 52
status: suggested
excerpt: 用測試集進行評估，針對個別錯誤 (FN) 進行探討；fn_indices = np.where((y_test == 1) & (y_pred == 0))[0]
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={52} status="suggested" excerpt={"用測試集進行評估，針對個別錯誤 (FN) 進行探討；fn_indices = np.where((y_test == 1) & (y_pred == 0))[0]"} client:visible />

```python
fn_indices = np.where((y_test == 1) & (y_pred == 0))[0]
```

醫學情境下 FN 是漏診，代價遠高於誤報，所以講義特別把這些樣本挑出來個別檢討，而不是只看總分。

最後用 `joblib.dump()` 把訓練好的最佳模型（指整條 pipeline）存成檔案，之後可直接載入評估。

{/* @ai-reference
id: save-load-model
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 53
status: suggested
excerpt: Save and Load the Model
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={53} status="suggested" excerpt={"Save and Load the Model"} client:visible />

## 作業三：手寫數字多類別分類器

**Assignment #3** 是低解析度手寫數字（0–9）的多類別分類器，情境是**光學字元辨識（optical character recognition, OCR）** 中已完成定位與切割後的字元辨識任務：

{/* @ai-reference
id: assignment3-intro
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 55
status: suggested
excerpt: 目的：低解析度手寫數字 (0-9) 多類別分類器；應用情境：光學字元辨識 (OCR)，假設字元定位與切割已經過預先處理
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={55} status="suggested" excerpt={"目的：低解析度手寫數字 (0-9) 多類別分類器；應用情境：光學字元辨識 (OCR)，假設字元定位與切割已經過預先處理"} client:visible />

| 項目 | 要求 |
| --- | --- |
| 資料 | `sklearn.datasets.load_digits()` ——1797 張 $8 \times 8$ 灰階影像、64 維像素特徵（整數 $[0, 16]$）、10 個類別 |
| 演算法 | Logistic Regression |
| 切分 | 80% 訓練（用於交叉驗證）、20% 測試 |
| 特徵選取 | 至少淘汰 20 個特徵，即只用 44 個以下的像素值 |
| 效能門檻 | 測試集（約 360 筆）整體正確率達 **95% 以上** |
| 模型儲存 | 用 `joblib.dump()` 存下最佳模型（指整條 pipeline） |

{/* @ai-reference
id: assignment3-dataset-row
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 56
status: suggested
excerpt: 1797 張 8×8 灰階 (grayscale) 影像；64 維（像素值）特徵（整數值 [0,16]）；多類別標籤 0–9 共 10 類
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={56} status="suggested" excerpt={"1797 張 8×8 灰階 (grayscale) 影像；64 維（像素值）特徵（整數值 [0,16]）；多類別標籤 0–9 共 10 類"} client:visible />

{/* @ai-reference
id: assignment3-requirements-row
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 57
status: suggested
excerpt: 以 Logistic Regression 作為分類器；切分比例 80% 訓練（用於交叉驗證）、20% 測試；至少淘汰 20 個以上的特徵；整體正確率達 95% 以上
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={57} status="suggested" excerpt={"以 Logistic Regression 作為分類器；切分比例 80% 訓練（用於交叉驗證）、20% 測試；至少淘汰 20 個以上的特徵；整體正確率達 95% 以上"} client:visible />

三項預期產出：

1. 特徵選取遮罩的視覺化（0 為淘汰、1 為選取，標題含選取比例）

{/* @ai-reference
id: assignment3-expected-result1
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 59
status: suggested
excerpt: 預期結果 1：視覺化（特徵選取遮罩）呈現特徵選取結果，0 淘汰、1 選取，標題含選取比例，必須在 44/64 以下
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={59} status="suggested" excerpt={"預期結果 1：視覺化（特徵選取遮罩）呈現特徵選取結果，0 淘汰、1 選取，標題含選取比例，必須在 44/64 以下"} client:visible />

2. 測試集的分類報告與混淆矩陣視覺化

{/* @ai-reference
id: assignment3-expected-result2
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 60
status: suggested
excerpt: 預期結果 2：輸出測試集的分類報告，視覺化呈現混淆矩陣，必須達 0.95 以上
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={60} status="suggested" excerpt={"預期結果 2：輸出測試集的分類報告，視覺化呈現混淆矩陣，必須達 0.95 以上"} client:visible />

3. 所有誤判樣本的圖（標註真實標籤與預測結果，且必須與混淆矩陣對應）

{/* @ai-reference
id: assignment3-expected-result3
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 61
status: suggested
excerpt: 預期結果 3：輸出誤判的樣本總數，繪出所有誤判的樣本，標示真實標籤與預測結果，必須與混淆矩陣對應
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={61} status="suggested" excerpt={"預期結果 3：輸出誤判的樣本總數，繪出所有誤判的樣本，標示真實標籤與預測結果，必須與混淆矩陣對應"} client:visible />

繳交規則要特別注意：只上傳**模型評估**部分的程式碼與模型檔（打包成單一 `.zip`），**不包含任何訓練或模型選取程式碼**——助教是直接執行評估程式批閱，沒有時間跑交叉驗證與微調。

{/* @ai-reference
id: assignment3-submission-rules
file: _references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf
page: 58
status: suggested
excerpt: 不要包含任何訓練或模型選取程式碼；助教將透過執行模型評估程式批閱作業（沒有時間進行交叉驗證與微調）
*/}
<PdfRefChip file="_references/機器學習實作系列/第7週-特徵工程與模型選擇評估.pdf" page={58} status="suggested" excerpt={"不要包含任何訓練或模型選取程式碼；助教將透過執行模型評估程式批閱作業（沒有時間進行交叉驗證與微調）"} client:visible />

## 教材預習重點

> 摘自 `machine-learning-book` 專案的 `STUDY_GUIDE_zh-TW.md`，對應章節：ch04、ch06。上課後請依實際授課內容調整。

**在做什麼**：ch04 處理真實資料常見的髒亂問題——缺值、類別型欄位編碼、特徵尺度不一致、特徵太多時怎麼選；ch06 是「怎麼知道模型真的有效」的方法論，包含交叉驗證、學習曲線判斷 bias/variance、超參數搜尋、各種評估指標。

**關鍵概念**：missing data 處理、one-hot encoding、train/test split、L1/L2 regularization、feature importance、pipeline、k-fold cross-validation、learning curve / validation curve、grid search / randomized search、confusion matrix、precision/recall、ROC curve、class imbalance

**PDSH 對照**：`PythonDataScienceHandbook` 的 `05.03`（Hyperparameters and Model Validation）、`05.04`（Feature Engineering）是這週主題的簡化版；缺值處理也可以對照 Pandas 篇的 `03.04`（Missing Values）。

## 關鍵定義

| 名詞 | 定義 |
| --- | --- |
| 超參數調整（hyperparameter tuning） | 狹義的模型選擇：對指定的演算法，選出調校參數的最佳值 |
| 演算法選擇（algorithm selection） | 廣義的模型選擇：比較不同演算法各自的最佳表現，選出最好的那個 |
| 保留法交叉驗證（holdout CV） | 把資料切成訓練、驗證、測試三份；缺點是結果對「訓練與驗證怎麼分」很敏感 |
| $k$ 折交叉驗證（k-fold CV） | 把訓練資料隨機不放回切成 $k$ 折，輪流以 1 折驗證、$k-1$ 折訓練，取 $k$ 次的平均 |
| 分層 $k$ 折（stratified k-fold） | 每一折中保持類別標籤的比例；分類問題應優先使用 |
| 留一交叉驗證（leave-one-out CV） | $k$ 等於訓練樣本數的極端情形 |
| 學習曲線（learning curve） | 訓練與驗證分數對**訓練資料量**的曲線；用來判斷再蒐集更多資料有沒有用 |
| 驗證曲線（validation curve） | 訓練與驗證分數對**某個超參數值**的曲線；用來挑該超參數 |
| 格狀搜尋（grid search） | 窮舉所有超參數組合；只要最佳解在網格內就保證找得到，但成本隨網格大小急遽上升 |
| 隨機搜尋（randomized search） | 從分布或離散集合抽樣固定次數；同樣預算下能探索更廣的取值範圍 |
| 逐次減半搜尋（successive halving） | 先用少量資源評估所有候選，反覆淘汰並把資源集中到存活者 |
| 巢狀交叉驗證（nested CV） | 外層切訓練／測試折、內層在訓練折上選模型；用於演算法選擇的不偏估計 |
| 混淆矩陣（confusion matrix） | 記錄 TP／TN／FP／FN 四種預測計數的方陣；所有分類指標的共同來源 |
| 真陽率 TPR（true positive rate） | 真實為陽性的樣本中被正確判為陽性的比例；等同召回率 |
| 偽陽率 FPR（false positive rate） | 真實為陰性的樣本中被誤判為陽性的比例 |
| 精確率（precision） | 判為陽性的樣本中真的是陽性的比例 |
| 召回率（recall） | 真實陽性中被抓出來的比例 |
| F1 分數 | 精確率與召回率的調和平均；只有兩者都高才會高 |
| 馬修斯相關係數（MCC） | 同時使用四格數字的相關係數，值域 $[-1,1]$；類別不平衡時比 F1 更不易被單邊表現誤導 |
| ROC 曲線 | 移動決策門檻所畫出的 FPR 對 TPR 軌跡；量的是排序能力而非單一門檻的表現 |
| ROC AUC | ROC 曲線下面積，範圍 $[0,1]$；完美為 1，隨機猜測為 0.5 |
| 微平均（micro-average） | 每個樣本等權，由樣本數多的類別主導 |
| 宏平均（macro-average） | 每個類別等權，適合類別不平衡的情形 |
| 類別不平衡（class imbalance） | 一或多個類別在資料集中被過度代表；正確率此時會嚴重誤導 |

## 公式

- 錯誤率：$\text{ERR}=\dfrac{FP+FN}{FP+FN+TP+TN}$
- 正確率：$\text{ACC}=\dfrac{TP+TN}{FP+FN+TP+TN}=1-\text{ERR}$
- 真陽率：$\text{TPR}=\dfrac{TP}{P}=\dfrac{TP}{FN+TP}$
- 偽陽率：$\text{FPR}=\dfrac{FP}{N}=\dfrac{FP}{FP+TN}$
- 精確率：$\text{PRE}=\dfrac{TP}{TP+FP}$
- 召回率：$\text{REC}=\text{TPR}=\dfrac{TP}{FN+TP}$
- F1 分數：$\text{F1}=2\cdot\dfrac{\text{PRE}\times\text{REC}}{\text{PRE}+\text{REC}}$
- 馬修斯相關係數：$\text{MCC}=\dfrac{TP\times TN-FP\times FN}{\sqrt{(TP+FP)(TP+FN)(TN+FP)(TN+FN)}}$
- $k$ 折交叉驗證的效能估計：$\overline{\text{score}}=\dfrac{1}{k}\sum_{i=1}^{k}\text{score}_i$
- 格狀搜尋的評估次數：$k\times\prod_{j}n_j$，其中 $n_j$ 為第 $j$ 個超參數的候選值個數、$k$ 為折數

## 考題

### 概念辨析題

#### 複習題 1

> 驗證集與測試集都用來「評估模型」，為什麼不能只留一份？

**詳解**

兩者評估的目的不同。驗證集參與**選擇**——挑超參數、比較演算法，我們會反覆用它衡量多個候選並挑出最好的那個。經過這個過程，被選中的模型已經是「在驗證集上表現最好」的模型，它的驗證分數包含了對這份特定資料的適應成分，因此偏樂觀。測試集的任務是給出**未參與任何決策**的泛化估計，所以必須全程封存，只在最後動一次。若只留一份，報告出來的分數就是「我挑了最會考這份考卷的學生，再用同一份考卷測他」，失去意義。

#### 複習題 2

> $k$ 折交叉驗證相對 holdout 的改善是什麼？為什麼資料量小時要用大的 $k$？

**詳解**

Holdout 的結果對「訓練集與驗證集怎麼分」非常敏感，換一種切法結論可能就變了——因為結論建立在單一次隨機切分上。$k$ 折讓每筆資料都輪流當過驗證資料，取 $k$ 次的平均，對子分割方式的敏感度因此大幅下降，而且還能同時得到標準差，看出估計的穩定程度。資料量小時要用大的 $k$，是因為每輪的訓練集大小是 $\frac{k-1}{k}$ 倍的原始資料：$k=2$ 時只剩一半拿來訓練，模型會因為資料太少而低估真實能力；$k$ 越大，每輪留給訓練的比例越接近全部，估計才不會被「訓練資料不足」污染。代價是要訓練 $k$ 次，成本隨 $k$ 線性上升。

#### 複習題 3

> 學習曲線呈現「兩條線都低且已貼合」與「兩條線之間有明顯的縫」，分別代表什麼？各該採取什麼行動？

**詳解**

兩條線都低且貼合是**高偏差、欠擬合**：模型連訓練資料的規律都抓不到，訓練分數與驗證分數一樣差，代表瓶頸在模型本身而不在資料量。此時再蒐集更多資料只是把同一條平線延長，沒有幫助；該做的是增加模型參數數量、降低正則化強度，或加入更有資訊量的特徵。兩條線之間有持續的縫是**高變異、過擬合**：模型把訓練資料背了下來，訓練分數高但驗證分數跟不上。此時蒐集更多訓練資料是有效的，也可以降低模型複雜度、提高正則化參數或減少特徵數。這張圖最實用的價值就是它能明確回答「該不該再去要資料」。

#### 複習題 4

> 格狀搜尋保證找到網格內的最佳組合，為什麼實務上常改用隨機搜尋？

**詳解**

格狀搜尋的問題不在「試得不夠多」，而在它被迫**在每個維度上都試同樣多次**。若有兩個超參數各給 5 個候選值，共 25 種組合——但其中一個超參數可能對效能幾乎沒有影響。在這種情況下，25 次評估實際上只在真正重要的那一軸上試了 5 個相異值，另外 20 次是在重複。隨機搜尋用同樣的 25 次預算從分布中抽樣，在重要維度上通常能試到接近 25 個相異值，取樣密度高得多，也能探索更廣的取值範圍。由於真正關鍵的超參數通常只有少數幾個，同樣預算下隨機搜尋往往更划算。

#### 複習題 5

> 為什麼比較不同演算法時需要巢狀交叉驗證？單層交叉驗證會高估什麼？

**詳解**

單層交叉驗證若同時用來挑超參數又用來報告分數，這個分數就被污染了：超參數是挑「在這些折上表現最好」的那一組，因此回報的最佳分數已經包含了對這批特定折的適應成分，會**高估泛化效能**。巢狀交叉驗證把兩件事分開：內層迴圈只在外層的訓練折上跑交叉驗證來選超參數，外層的測試折完全沒有參與選擇，用它評估才得到不偏的估計。這樣算出來的，才是「我們調完超參數、拿去面對沒看過的資料」時可以合理期待的表現。講義的 $5\times2$ 交叉驗證就是外層 5 折、內層 2 折。

#### 複習題 6

> 一份健檢資料只有 1% 為陽性，模型永遠預測陰性可得 99% 正確率。請說明應該改看哪些指標，以及為什麼。

**詳解**

這是典型的類別不平衡。永遠預測陰性的模型 $TP = 0$、$FP = 0$，正確率確實是 99%，但它對真正的目標——找出病患——毫無作用。該看的是**召回率**：$\text{REC} = \frac{TP}{FN+TP} = 0$，立刻暴露問題；精確率在 $TP + FP = 0$ 時無定義，同樣反映模型根本沒做出任何陽性判斷。也可以看 MCC，它同時吃四格數字，此時分子 $TP \times TN - FP \times FN = 0$，MCC 為 0，等同隨機。除了換指標，還可以對少數類過採樣、對多數類欠採樣，或設定 `class_weight='balanced'` 讓少數類的錯誤受到更重的懲罰。

#### 複習題 7

> 精確率與召回率為什麼會此消彼長？癌症篩檢與垃圾信過濾該各自偏向哪一個？

**詳解**

兩者共用同一個決策門檻。門檻調鬆時，更多樣本被判為陽性：真陽性增加使召回率上升，但偽陽性也一起增加，使精確率下降；門檻調緊則相反。這是同一組預測分數的重新切割，不是模型變好或變壞。選擇取決於兩種錯誤的代價：癌症篩檢的漏診（FN）代價遠高於誤報（FP，頂多多做一次複檢），因此**偏向召回率**，寧可門檻鬆一點；垃圾信過濾誤殺一封重要信件（FP）的代價高於漏掉一封垃圾信（FN），因此**偏向精確率**，門檻要嚴。

#### 複習題 8

> 微平均與宏平均的差別是什麼？類別不平衡時該選哪一個？

**詳解**

差別在權重給誰。微平均把每一個**樣本**視為等權，先把所有類別的 TP、FP、FN 加總再計算指標，因此樣本數多的類別會主導結果。宏平均把每一個**類別**視為等權，先算出每個類別各自的指標再取平均，稀有類別與常見類別的發言權相同。類別不平衡時應選宏平均：若少數類只佔 1%，微平均幾乎完全反映多數類的表現，模型在少數類上表現再差也看不出來；宏平均則會讓少數類的低分明顯拉低平均值。另外要注意，在 `classification_report` 中 micro avg 等同於正確率，且各指標值會相同，因為總 FP 等於總 FN。

### 計算題

> 以下的數值為自行設計的練習題，用來套用本週的公式，並非講義原有的習題。

#### 複習題 1

> 某二元分類器在測試集上的混淆矩陣為 $TP = 40$、$FN = 10$、$FP = 20$、$TN = 130$。請計算 ACC、PRE、REC 與 F1。

**詳解**

$$
\begin{aligned}
\text{ACC} &= \frac{TP+TN}{TP+TN+FP+FN} = \frac{40+130}{200} = 0.85 \\[6pt]
\text{PRE} &= \frac{TP}{TP+FP} = \frac{40}{40+20} = \frac{40}{60} \approx 0.667 \\[6pt]
\text{REC} &= \frac{TP}{TP+FN} = \frac{40}{40+10} = \frac{40}{50} = 0.8 \\[6pt]
\text{F1}  &= 2\cdot\frac{0.667 \times 0.8}{0.667 + 0.8} \approx 2\cdot\frac{0.5336}{1.467} \approx 0.727
\end{aligned}
$$

#### 複習題 2

> 沿用上題的四格數字，請計算 TPR、FPR 與 MCC。

**詳解**

$$
\begin{aligned}
\text{TPR} &= \frac{TP}{FN+TP} = \frac{40}{50} = 0.8 \\[6pt]
\text{FPR} &= \frac{FP}{FP+TN} = \frac{20}{20+130} = \frac{20}{150} \approx 0.133 \\[6pt]
\text{MCC} &= \frac{40 \times 130 - 20 \times 10}{\sqrt{(40+20)(40+10)(130+20)(130+10)}} \\[6pt]
&= \frac{5200 - 200}{\sqrt{60 \times 50 \times 150 \times 140}} = \frac{5000}{\sqrt{63{,}000{,}000}} \\[6pt]
&\approx \frac{5000}{7937} \approx 0.630
\end{aligned}
$$

#### 複習題 3

> 某健檢資料 1000 筆中有 10 筆陽性。模型永遠預測陰性，請寫出其混淆矩陣四格並計算 ACC 與 REC，說明這個例子的意義。

**詳解**

永遠預測陰性代表沒有任何樣本被判為陽性，因此 $TP = 0$、$FP = 0$；10 筆真陽性全部被漏掉（$FN = 10$），990 筆真陰性全部判對（$TN = 990$）。

$$
\begin{aligned}
\text{ACC} &= \frac{0+990}{1000} = 0.99 \\[6pt]
\text{REC} &= \frac{0}{0+10} = 0
\end{aligned}
$$

**考的觀念**：99% 的正確率與 0 的召回率同時成立。正確率被多數類完全主導，對不平衡問題毫無鑑別力；召回率一眼就揭穿模型根本沒有找出任何病患。精確率此時分母為 0 而無定義，同樣是模型未做出任何陽性判斷的徵兆。

#### 複習題 4

> 使用 5 折交叉驗證搭配格狀搜尋，超參數 `C` 有 6 個候選值、`gamma` 有 4 個候選值。請計算總共要訓練幾次模型。若改用只抽 10 組的隨機搜尋，又是幾次？

**詳解**

$$
\begin{aligned}
\text{格狀搜尋} &= k \times n_C \times n_\gamma = 5 \times 6 \times 4 = 120 \\[6pt]
\text{隨機搜尋} &= k \times n_{\text{iter}} = 5 \times 10 = 50
\end{aligned}
$$

**考的觀念**：格狀搜尋的 24 種組合中，若 `gamma` 其實對效能影響很小，這 120 次訓練實際上只在 `C` 上試了 6 個相異值；隨機搜尋的 10 組抽樣則可能在 `C` 上試到接近 10 個相異值，用不到一半的成本得到更密的有效取樣。

#### 複習題 5

> 使用外層 5 折、內層 2 折的巢狀交叉驗證，內層對 3 個超參數組合做格狀搜尋。請計算總共訓練幾次模型（不含最後在完整訓練集上的重訓）。

**詳解**

$$
\begin{aligned}
\text{每個外層折的內層次數} &= k_{\text{inner}} \times n_{\text{grid}} = 2 \times 3 = 6 \\[6pt]
\text{外層 5 折合計} &= 5 \times 6 = 30
\end{aligned}
$$

若外層每折在選出最佳組合後還要用該折的訓練資料重訓一次再評估，則再加 $5$ 次，共 $35$ 次。

**考的觀念**：巢狀交叉驗證的成本是兩層相乘，這正是它雖然正確卻不常被用在大型搜尋上的原因——實務上通常先用單層交叉驗證縮小候選範圍，再對少數幾個演算法做巢狀比較。
