---
title: "專案管理系列：第五章 - Project Mgmt. Tool 專案管理工具"
description: 四種專案管理工具的分工與串接——WBS 拆範圍、Gantt 排時程、Kanban 追流動、Task 記執行；涵蓋 100% Rule、WIP 限制與用時間欄位反推流程瓶頸。
tags: ["AI 陪跑筆記", "專案管理"]
createdAt: "2026-06-14"
updatedAt: "2026-06-21"
---

# Mgmt. Tool（管理工具）

前面的筆記談的大多是觀念——專案與產品的差異、方法論的選擇、角色職責的分工，以及 Waterfall 的流程。這一章會將這些觀念轉成可落地執行的具體作法，也就是使用專案管理工具。

下面是專案管理常用的工具，實際上當然不只這些，下面幾項工具只是根據之前的經驗統整出來，且可以按層級區分使用情境的部分：

| 工具 | 回答的問題 | 層級 |
| --- | --- | --- |
| :tip[**WBS**]{content="Work Breakdown Structure，工作分解結構：把交付物由上而下層層拆解成可估時、可指派的工作單元。"} | 要做哪些事？（範圍） | 規劃 |
| :tip[**工作包**]{content="Work Package：WBS 拆到最底的葉節點，是「範圍」的最小單位，以成果命名（如「會員系統」），排程時再展開成多個 Task。"} | 範圍拆到最小是哪一塊？（最小交付單位） | 拆解 |
| :tip[**Gantt**]{content="甘特圖：用時間軸排出各任務的起訖與先後依賴，看整體時程。"} | 什麼時候做、誰先誰後？（時程與依賴） | 排程 |
| :tip[**Kanban**]{content="看板：用欄位視覺化工作流、限制在製品（WIP），讓任務順暢流動、瓶頸現形。"} | 現在進行到哪了？（流動狀態） | 追蹤 |
| :tip[**Task**]{content="任務：由上一層工作包展開出的執行單元，以動作命名（如「設計會員資料表」），記錄執行的人、時間與狀態等細節；一個工作包通常對應多個 Task。"} | 這件事的細節與紀錄是什麼？（執行） | 執行 |

---

## WBS（Work Breakdown Structure）

:::note
工作分解結構：把專案的最終交付物，**由上而下層層拆解**成可被管理、估時、指派的工作單元；最末端的單元嚴格來說稱為 **Work Package（工作包）**。
:::

### 結構特性

- **樹狀結構**：專案總目標 → 主要交付物 → 子交付物 → 工作包
- **100% Rule**：子節點總和必須等於父節點的全部範圍，同時也包含總工時
- **以「交付物 / 成果」為導向**，而非以「動作」為導向（避免拆成一堆動詞清單）

### 拆解的粒度

一個末端工作包的工時建議落在 **8 ～ 80 小時**之間：

- **太大** → 難以估時與追蹤，風險被掩蓋
- **太細** → 管理成本超過實際產出價值
- **理想** → 能被一個負責人扛起，且驗收標準明確

### 為什麼 PM 需要 WBS

- **確認範圍（Scope）**：未列入 WBS 的東西，預設就不在這次專案內
- **估算的基礎**：沒有拆解就沒有估時與成本估算
- **風險識別**：拆解過程會浮現原本沒想到的依賴與盲點
- **指派責任**：每個工作包都應有明確負責人（呼應 [專案管理系列：第三章 - Role & Responsibility (R&R)](./role-responsibility-rr) RACI 中的 **R**）
- **承接後續工具**：Gantt、Kanban、Task 都是以末端工作包為基礎再展開

### 範例：DutyMate 值日生排班系統

以我們現在在跑的 Side Project「DutyMate」（值日生排班 App）為例，把 :tip[PRD]{content="Product Requirement Document，產品需求文件：在動工前定義專案要做什麼的依據文件，通常包含問題背景、目標、使用者角色、功能規格與驗收標準、時程等。它界定了專案範圍，正好是 WBS 拆解的輸入來源。"} 的範圍拆成 WBS：

```
DutyMate 值日生排班系統
├── 1. 需求與規劃
│   ├── 1.1 痛點訪談與需求確認
│   ├── 1.2 PRD 定稿與待釐清項確認
│   └── 1.3 範圍與里程碑確認
├── 2. 設計
│   ├── 2.1 業務流程與 Use Case
│   ├── 2.2 資料庫 Schema 設計
│   ├── 2.3 API 規格設計
│   └── 2.4 UI Mockup
├── 3. 開發
│   ├── 3.1 基礎建設與權限（專案骨架、登入、用戶管理、角色權限）
│   ├── 3.2 活動設定與排除時段（活動設定、排除時段、排班限制表）
│   ├── 3.3 草稿班表與推薦演算法（推薦演算法、排班統計、班表發布）
│   ├── 3.4 正式班表與檢視（篩選值日生、工作任務備註、無法出勤標記）
│   └── 3.5 出勤統計與過往班表匯入
├── 4. 測試
│   ├── 4.1 單元測試（Jest）
│   ├── 4.2 系統整合測試（SIT）
│   └── 4.3 使用者驗收測試（UAT）
└── 5. 上線
    ├── 5.1 Docker 部署設定
    ├── 5.2 Cutover 與 Go-Live
    └── 5.3 上線後監控與保固
```

:::tip
通常在使用 WBS 時會搭配 Gantt 來排程，並直接在 WBS 上加上時間欄位（開始日、工時）來反推時程；也會搭配 Task 來記錄每個任務的細節與討論紀錄。
:::

我個人習慣在 WBS 上直接加上**負責人（PIC）** 欄位，還有**進度**欄位。這樣在排程與指派時就能一併完成。下面的示意圖就是結合 WBS 與 Gantt 的樣子，
但礙於版面限制，只留工作項目、PIC、工時和開始日期的欄位：

{/* @ai-visualize
id: wbs-simulator
type: free
prompt: |
  做一個互動式「WBS 拆解模擬器」，讓讀者親手體驗工作分解結構，而不只是讀文字。

  核心洞察：WBS 的價值在「拆到剛好」——只有末端的工作包（葉節點）才會被估時與執行，
  而每一層的子節點加總必須等於父節點的全部範圍（100% Rule），同層之間互斥窮盡（MECE）。

  互動設計（以上方「電商網站」WBS 為預設資料）：
  - 以可展開／收合的樹狀結構呈現專案 → 主要交付物 → 子交付物 → 工作包，點節點可展開收合。
  - 讓讀者對任一葉節點輸入「預估工時」，元件即時依 8/80 法則標示：
    落在 8~80 小時顯示綠色（理想）、低於 8 顯示「太細、管理成本過高」、高於 80 顯示「太大、風險被掩蓋」，
    用顏色與一行提示回饋，不用 emoji（改用 lucide-react 的 icon）。
  - 清楚以視覺區分「葉節點＝可估時可執行的工作包」與「上層節點＝只是分類彙總、不被執行」
    （例如上層節點顯示為唯讀的彙總工時，數值由子節點加總得出，藉此暗示 100% Rule）。
  - 刪掉某個工作包後，父節點彙總範圍隨之減少——具象化「未列入 WBS 就不在範圍內」。
  - 每個工作包（葉節點）可指定並編輯 PIC（負責人）。
  - WBS 與甘特圖整合成「同一張甘特表」：左側為凍結欄位（工作項目名稱、PIC、工時、開始日期、刪除），
    右側為可橫向捲動的時間軸；左右逐列對齊，展開／收合／刪除同步影響兩側。
  - 開始日期可編輯；結束日期不可編輯，自動由「開始日期 + 工時換算天數」算出（8 小時 = 1 天，天數 = ceil(工時/8)）。
    葉節點長條依工時長度與 8/80 狀態（綠/琥珀/紅）著色；父（彙總）節點顯示橫跨其子工作包最早～最晚的 rollup 彙總細條。
  - 8/80 提示不直接佔用列空間：在工作項目名稱前以彩色狀態 icon 呈現，點擊 icon 彈出 tooltip 顯示完整建議。
  底層以攤平的可見列清單驅動左右兩面板對齊，react state 驅動展開／工時／PIC／日期，縮排線為單一中性淡灰細線，
  甘特圖與 tooltip 皆手寫（不額外引入函式庫），motion 控制列淡出，並尊重 prefers-reduced-motion。
caption: 整合式 WBS 甘特表：凍結欄位估時／指派 PIC／編輯開始日，右側時間軸自動排程，狀態 icon 點擊看 8/80 建議
status: generated
*/}

import GeneratedFrame from '@/components/GeneratedFrame.astro'
import WbsSimulator from '@/components/generated/wbs-simulator'

<GeneratedFrame id="wbs-simulator" type="free" prompt={"做一個互動式「WBS 拆解模擬器」，讓讀者親手體驗工作分解結構，而不只是讀文字。\n\n核心洞察：WBS 的價值在「拆到剛好」——只有末端的工作包（葉節點）才會被估時與執行，\n而每一層的子節點加總必須等於父節點的全部範圍（100% Rule），同層之間互斥窮盡（MECE）。\n\n互動設計（以上方「電商網站」WBS 為預設資料）：\n- 以可展開／收合的樹狀結構呈現專案 → 主要交付物 → 子交付物 → 工作包，點節點可展開收合。\n- 讓讀者對任一葉節點輸入「預估工時」，元件即時依 8/80 法則標示：\n  落在 8~80 小時顯示綠色（理想）、低於 8 顯示「太細、管理成本過高」、高於 80 顯示「太大、風險被掩蓋」，\n  用顏色與一行提示回饋，不用 emoji（改用 lucide-react 的 icon）。\n- 清楚以視覺區分「葉節點＝可估時可執行的工作包」與「上層節點＝只是分類彙總、不被執行」\n  （例如上層節點顯示為唯讀的彙總工時，數值由子節點加總得出，藉此暗示 100% Rule）。\n- 刪掉某個工作包後，父節點彙總範圍隨之減少——具象化「未列入 WBS 就不在範圍內」。\n- 每個工作包（葉節點）可指定並編輯 PIC（負責人）。\n- WBS 與甘特圖整合成「同一張甘特表」：左側為凍結欄位（工作項目名稱、PIC、工時、開始日期、刪除），右側為可橫向捲動的時間軸；左右逐列對齊，展開／收合／刪除同步影響兩側。\n- 開始日期可編輯；結束日期不可編輯，自動由「開始日期 + 工時換算天數」算出（8 小時 = 1 天，天數 = ceil(工時/8)）。葉節點長條依工時長度與 8/80 狀態（綠/琥珀/紅）著色；父（彙總）節點顯示橫跨其子工作包最早～最晚的 rollup 彙總細條。\n- 8/80 提示不直接佔用列空間：在工作項目名稱前以彩色狀態 icon 呈現，點擊 icon 彈出 tooltip 顯示完整建議。\n底層以攤平的可見列清單驅動左右兩面板對齊，react state 驅動展開／工時／PIC／日期，縮排線為單一中性淡灰細線，甘特圖與 tooltip 皆手寫（不額外引入函式庫），motion 控制列淡出，並尊重 prefers-reduced-motion。"} caption="整合式 WBS 甘特表：凍結欄位估時／指派 PIC／編輯開始日，右側時間軸自動排程，狀態 icon 點擊看工時建議">
  <WbsSimulator client:visible />
</GeneratedFrame>

這邊也提供了一個用 Excel 建立的 WBS 模板，已經套用好基礎欄位，未來有機會使用到也可以下載

import DownloadButton from '@/components/DownloadButton.astro'

<DownloadButton title="下載 WBS 範本" href="/downloads/20260616 WBS-template.xlsx" color="blue" variant="outline" icon="download" />

<div class="mt-3">
  另外 Excel 官網也提供了其他的範本可以參考，可以訪問 [Free Gantt charts customizable templates online](https://excel.cloud.microsoft/create/en/gantt-charts/)
</div>

---

## Kanban（看板）

:::note
源自豐田生產系統，核心是**視覺化工作流**與**限制在製品數量（WIP, Work In Progress）**，目標是讓任務在流程中順暢流動，並讓瓶頸無所遁形。
:::

### 基本元素

| 元素 | 說明 |
| --- | --- |
| **Board（看板）** | 代表整個工作流的板子 |
| **Column（欄位）** | 對應工作流的每個階段，如 To Do / In Progress / Review / Done |
| **Card（卡片）** | 對應一個 :tip[Task]{content="任務：最末端、可被執行與追蹤的工作單元，記錄負責人、時間與狀態等細節。本篇最後一節「Task（任務）」會專門介紹。"}，在欄位之間由左往右流動 |
| **WIP Limit** | 每個欄位同時可存在的卡片數量上限 |
| **Swimlane（泳道）** | 橫向切分，區分不同類型工作（如 新功能 / Bug / 技術債）或不同產品線 |

### 核心精神

- **視覺化（Visualize）**：所有人一眼看到目前狀態與卡關處
- **限制 WIP**：強迫團隊「先完成、再開始」，避免同時推太多卻沒一件結案
- **管理流動（Manage Flow）**：關注 :tip[**Lead Time**]{content="前置時間：從任務「被建立 / 提出」到「完成交付」的總耗時，反映的是需求方（顧客）從開口到拿到成果一共等了多久。"} 與 :tip[**Cycle Time**]{content="週期時間：從「實際開始動工」到「完成」的耗時，反映團隊真正處理一件事的速度；它是 Lead Time 的子區段（扣掉排隊等待的部分）。"}。後面的章節會專門講指標，這裡只先提一下它們是 Kanban 的核心指標，用來反推流程上的瓶頸與改善效果
- **明確的流程政策**：每個欄位的**進入條件**與**完成的定義**要寫清楚，例如說，推進到 Done 的條件是開發團隊完成功能開發與交付技術文件，與 PM 通過功能測試，而不是「我覺得我做完了」
- **持續改善**：定期檢視瓶頸、調整欄位與 WIP 上限

:::note{collapsible title="我的習慣：先別急著卡死 WIP"}
我自己的習慣不會特別去限制 WIP，因為可以透過 Kanban 看目前大部分的任務**堆積**在哪個階段，另外也可以透過 Lead Time / Cycle Time 的數據來反推流程上的瓶頸在哪裡；如果一開始就把 WIP 卡死，反而會失去這些訊號。
:::

### 為什麼 PM 適合用 Kanban

- **進度透明**：不必開會問進度，看板本身就是進度
- **瓶頸可視化**：哪個欄位卡片堆積，就是當前瓶頸（通常是 Review 或驗收）
- **與 WBS 搭配**：WBS 末端工作包展開出的 :tip[Task]{content="任務：最末端、可被執行與追蹤的工作單元，記錄負責人、時間與狀態等細節。本篇最後一節「Task（任務）」會專門介紹。"}，可一張張變成卡片直接進入流動

:::tip{title="實務小提醒"}
- **WIP Limit** 一開始設寬一點，再依實際狀況收斂，比一開始就卡死好
- **Done** 一定要有明確驗收標準，否則會變成「我覺得我做完了」
- 卡片**一定要有負責人**，沒人負責的卡片等於沒人在推進
- 定期清理卡片，長期沒被動到的卡片要嘛刪掉、要嘛降級成想法清單，否則會變得混亂難以管理
:::

### 我的看板狀態設計

我自己的 Kanban :tip[Task]{content="任務：最末端、可被執行與追蹤的工作單元，記錄負責人、時間與狀態等細節。本篇最後一節「Task（任務）」會專門介紹。"} 的狀態流，
會按以下方式設計：

```
Open ──→ On Going ──→ Done ──→ Verified ──→ Closed
(待辦)    (進行中)    (待驗收)   (待結案)     (已結案)
```

:::info
**Pending** 與 **Cancelled** 我不放成欄位，而是用卡片標籤或泳道處理——因為它們不是流程上的正常往前推進，而是「暫時離開流動」或「退出流動」的狀態。
:::

{/* @ai-visualize
id: kanban-simulator
type: free
prompt: |
  做一個可實際操作的「Kanban 看板模擬器」，讓讀者親手拖動卡片、撞到 WIP 上限、看見瓶頸浮現，
  藉操作熟悉看板的運作，而不只是讀規則。

  核心洞察：Kanban 的兩大張力是「視覺化流動」與「限制在製品（WIP）」——限制 WIP 會強迫團隊
  「先完成、再開始」，而卡片在哪一欄堆積，哪裡就是瓶頸（通常是 Review／驗收）。

  互動設計（欄位採作者自訂的狀態流）：
  - 欄位由左到右為 Open → On Going → Done → Verified → Closed，每欄顯示標題與中文副標
    （待辦／進行中／待驗收／待結案／已結案）。
  - 預先放幾張示範卡片（每張有標題與負責人 avatar 字母），讀者可用拖放或點「往右推進」按鈕讓卡片流動。
  - 每欄設 WIP Limit（可由讀者用 +/- 調整，預設例如 In Progress=2、Review=2）；
    當某欄卡片數達到上限，該欄標頭顯示計數變紅、並擋下再被推入的卡片，給出「先完成、再開始」的提示。
  - 當任一欄卡片堆積（接近或達到上限），自動把該欄高亮為「瓶頸」，呼應「看板讓瓶頸無所遁形」。
  - Pending／Cancelled 不做成欄位，而是用「卡片標籤 + 獨立泳道」處理：標記後卡片離開欄位、
    滑入看板下方一條獨立的「離開流動」泳道；Pending 帶琥珀「暫離流動」標籤（可回到流動），
    Cancelled 帶灰色「退出流動」標籤（標題刪除線、可重新開啟）。兩者都不佔欄位 WIP，
    具象化「暫時離開流動」與「退出流動」並非正常往前推進。
  - 可選：簡單顯示一張卡片從 Open 進入到 Closed 的 Lead Time 計數，呼應「管理流動」。
  欄位與卡片用 Tailwind 排版，拖放與狀態用 react state、欄位↔泳道移動用 motion(LayoutGroup)；尊重 prefers-reduced-motion。
caption: 可操作的 Kanban 模擬器：拖動卡片流動、調整 WIP 上限、讓瓶頸現形，並把 Pending／Cancelled 移入「離開流動」泳道
status: generated
*/}

import KanbanSimulator from '@/components/generated/kanban-simulator'

<GeneratedFrame id="kanban-simulator" type="free" prompt={"做一個可實際操作的「Kanban 看板模擬器」，讓讀者親手拖動卡片、撞到 WIP 上限、看見瓶頸浮現，\n藉操作熟悉看板的運作，而不只是讀規則。\n\n核心洞察：Kanban 的兩大張力是「視覺化流動」與「限制在製品（WIP）」——限制 WIP 會強迫團隊\n「先完成、再開始」，而卡片在哪一欄堆積，哪裡就是瓶頸（通常是 Review／驗收）。\n\n互動設計（欄位採作者自訂的狀態流）：\n- 欄位由左到右為 Open → On Going → Done → Verified → Closed，每欄顯示標題與中文副標\n  （待辦／進行中／待驗收／待結案／已結案）。\n- 預先放幾張示範卡片（每張有標題與負責人 avatar 字母），讀者可用拖放或點「往右推進」按鈕讓卡片流動。\n- 每欄設 WIP Limit（可由讀者用 +/- 調整，預設例如 In Progress=2、Review=2）；\n  當某欄卡片數達到上限，該欄標頭顯示計數變紅、並擋下再被推入的卡片，給出「先完成、再開始」的提示。\n- 當任一欄卡片堆積（接近或達到上限），自動把該欄高亮為「瓶頸」，呼應「看板讓瓶頸無所遁形」。\n- Pending／Cancelled 不做成欄位，而是用「卡片標籤 + 獨立泳道」處理：標記後卡片離開欄位、滑入看板下方一條獨立的「離開流動」泳道；Pending 帶琥珀「暫離流動」標籤（可回到流動），Cancelled 帶灰色「退出流動」標籤（標題刪除線、可重新開啟）。兩者都不佔欄位 WIP，具象化「暫時離開流動」與「退出流動」並非正常往前推進。\n- 可選：簡單顯示一張卡片從 Open 進入到 Closed 的 Lead Time 計數，呼應「管理流動」。\n欄位與卡片用 Tailwind 排版，拖放與狀態用 react state、欄位↔泳道移動用 motion(LayoutGroup)；尊重 prefers-reduced-motion。"} caption="可操作的 Kanban 模擬器：拖動卡片流動、調整 WIP 上限、讓瓶頸現形，並把 Pending／Cancelled 移入「離開流動」泳道">
  <KanbanSimulator client:visible />
</GeneratedFrame>

---

## Task（任務）

一個任務需要包含足夠資訊，才有助於追蹤管理。


<div className="w-full h-fit grid grid-cols-2 grid-rows-1 gap-4">
  <div>
    ### 時間欄位

    - 預計完成起訖時間
    - 實際完成起訖時間
    - 驗收起訖時間
    - 上線時間
  </div>
  <div>
    ### 人員欄位

    - 需求提案人
    - 需求審核人
    - 執行人
    - 驗收人
    - 上線審核人
  </div>
</div>

### 狀態

一般作法較精簡，推到「已完成」即收尾；我自己則把「驗收」與「結案」獨立成可追蹤的狀態。

::::tabs
:::tab{label="一般作法"}

| 狀態 | 說明 |
| --- | --- |
| **To Do** | 已建立，但還沒開始 |
| **In Progress** | 正在進行中，尚未完成 |
| **Done** | 已完成，且通過驗收 |

:::
:::tab{label="我的習慣"}

| 狀態 | 說明 |
| --- | --- |
| **Open** | 任務建立了，但還沒開始 |
| **On Going** | 正在進行中，尚未完成 |
| **Done** | 已完成，等待驗收 |
| **Verified** | 驗收通過，等待結案 |
| **Closed** | 驗收通過，正式結案 |
| **Pending** | 暫停（有問題待解，或優先級調整） |
| **Cancelled** | 取消（有更好方案，或資源不足所以取消） |

:::
::::

### 兩套狀態的對照

這兩套其實是**同一條流程**，差別在後段的細緻度。一般的狀態是任務推到「Done」就結束，因為 Done 的狀態表示驗收與結案被涵括在內進行處理。

| 我的習慣 | 一般作法 | 說明 |
| --- | --- | --- |
| Open | To Do | 已建立、尚未開始，完全對應 |
| On Going | In Progress | 進行中，完全對應 |
| Done | Done | 開發完成、待驗收；但我的 Done 只是中段而非終點 |
| Verified | *（包含在 Done 的階段中）* | 驗收通過，不另立狀態 |
| Closed | *（包含在 Done 的階段中）* | 正式結案，不另立狀態 |

換句話說，我多出來的 **Verified** 與 **Closed**，是把壓在「Done」裡的「驗收」與「結案」獨立拉出來成為可追蹤的狀態。
代價是流程看起來變長，好處是「完成 → 驗收」「驗收 → 上線」這兩段耗時能被量化，瓶頸才看得見。

### 為什麼要這麼多時間欄位

對使用者而言，最關心的往往只有一個時間點——**什麼時候上線**。但對 PM 來說，上線時間只是其中一個要素；
若把眼光拉到產品的長期推進，重點就不只是「準時上線」，而是讓**每一次的規劃都比上一次更準**。

這正是為什麼光是「完成時間」，都要拆成「**預計**」與「**實際**」兩欄：兩者的差距就是估時誤差，
長期記錄下來，團隊才能逐步縮小落差，讓往後的時程估算越來越可靠。

把時間欄位攤開後，真正的價值在於**反推每一段的耗時，找出流程瓶頸**：

| 區間 | 反映的問題 |
| --- | --- |
| **建立 → 開始**（Open → On Going） | 問題還在釐清、資源不足或優先級不清；等待越久代表前置壅塞越嚴重 |
| **開始 → 完成**（實際完成起訖） | 執行的工作量與估時準確度；與「預計」對照即估時誤差來源 |
| **完成 → 驗收**（Done → Verified） | 驗收方的負荷；常被忽略卻最容易卡關（驗收人沒空、標準不清） |
| **驗收 → 上線**（Verified → 上線） | 上線審核或部署排程卡關 |

當能看出「卡的不是開發、而是驗收」或「估時總是低估」這類**模式**時，這些時間欄位才從單純的紀錄，變成可以拿來**改善流程與校正估時**的依據。

{/* @ai-visualize
id: time-fields-bottleneck
type: free
prompt: |
  做一個可互動的「時間欄位 vs 瓶頸定位」對比走查，讓讀者親身體會：
  為什麼要記這麼多時間欄位——欄位愈細，愈能指出瓶頸具體卡在哪一段。

  核心洞察：同樣的「總耗時」，只記錄上線時間（合併視角）只能看到「慢」，
  卻看不出時間花在哪；把時間欄位攤開成四段後，瓶頸那一段會立刻凸顯出來。

  互動設計：
  - 一條任務生命週期時間軸，里程碑由左到右：建立 → 開始 → 完成 → 驗收 → 上線
    （對應 Open → On Going → Done → Verified → 上線），中間形成四個耗時區間：
      1. 建立→開始（排隊／前置壅塞）
      2. 開始→完成（執行／估時準確度，含預計 vs 實際）
      3. 完成→驗收（驗收方負荷）
      4. 驗收→上線（上線審核與發布窗口）
  - 提供「視角切換」：
      合併視角：只顯示一條灰色長條 + 「總耗時 N 天」，並提示「只知道慢，看不出卡在哪」。
      攤開視角：同樣的總長度切成四段，寬度與各段天數成比例；最長（瓶頸）那段以紅色強調、
               標上「瓶頸」徽章與 AlertTriangle icon，其餘段落以中性／淡藍呈現，各段標示天數與佔比。
    切換時用 motion 讓「一條長條 ←→ 分裂成四段」有過場，凸顯張力。
  - 提供「情境切換」（數個情境，總耗時相近但瓶頸不同），例如：
      前置壅塞（建立→開始 久）、估時失準（開始→完成 遠超預計）、驗收塞車（完成→驗收 特別長）、
      上線延宕（驗收→上線 久）。切換情境時各段寬度以 motion 平滑變化，瓶頸段隨之移動。
  - 在「估時失準」等情境，於『開始→完成』段內畫出「預計」基準線（虛線標記），對照「實際」超出多少，
    呼應內文「完成時間分成預計與實際」的用意。
  - 下方診斷卡：顯示目前情境的瓶頸段名稱 + 一行對應問題（取自內文表格，如「驗收方負荷過重：驗收人沒空、
    標準不清，最容易被忽略卻最卡關」），並用一句收斂：同樣 N 天，合併只看到『慢』，攤開才指得出卡在哪一段。
  時間軸與長條用手寫排版（SVG 或 div 比例條），react state 管理情境與視角，motion 控制過場；
  不用 emoji（一律 lucide-react icon），尊重 prefers-reduced-motion。
caption: 時間欄位愈細，瓶頸愈清楚：切換情境與「合併／攤開」視角，看同樣的總耗時如何指出不同的卡點
status: generated
*/}

import TimeFieldsBottleneck from '@/components/generated/time-fields-bottleneck'

<GeneratedFrame id="time-fields-bottleneck" type="free" prompt={"做一個可互動的「時間欄位 vs 瓶頸定位」對比走查，讓讀者親身體會：\n為什麼要記這麼多時間欄位——欄位愈細，愈能指出瓶頸具體卡在哪一段。\n\n核心洞察：同樣的「總耗時」，只記錄上線時間（合併視角）只能看到「慢」，\n卻看不出時間花在哪；把時間欄位攤開成四段後，瓶頸那一段會立刻凸顯出來。\n\n互動設計：\n- 一條任務生命週期時間軸，里程碑由左到右：建立 → 開始 → 完成 → 驗收 → 上線\n  （對應 Open → On Going → Done → Verified → 上線），中間形成四個耗時區間：\n    1. 建立→開始（排隊／前置壅塞）\n    2. 開始→完成（執行／估時準確度，含預計 vs 實際）\n    3. 完成→驗收（驗收方負荷）\n    4. 驗收→上線（上線審核與發布窗口）\n- 提供「視角切換」：\n    合併視角：只顯示一條灰色長條 + 「總耗時 N 天」，並提示「只知道慢，看不出卡在哪」。\n    攤開視角：同樣的總長度切成四段，寬度與各段天數成比例；最長（瓶頸）那段以紅色強調、\n             標上「瓶頸」徽章與 AlertTriangle icon，其餘段落以中性／淡藍呈現，各段標示天數與佔比。\n  切換時用 motion 讓「一條長條 ←→ 分裂成四段」有過場，凸顯張力。\n- 提供「情境切換」（數個情境，總耗時相近但瓶頸不同），例如：\n    前置壅塞（建立→開始 久）、估時失準（開始→完成 遠超預計）、驗收塞車（完成→驗收 特別長）、\n    上線延宕（驗收→上線 久）。切換情境時各段寬度以 motion 平滑變化，瓶頸段隨之移動。\n- 在「估時失準」等情境，於『開始→完成』段內畫出「預計」基準線（虛線標記），對照「實際」超出多少，\n  呼應內文「完成時間分成預計與實際」的用意。\n- 下方診斷卡：顯示目前情境的瓶頸段名稱 + 一行對應問題（取自內文表格，如「驗收方負荷過重：驗收人沒空、\n  標準不清，最容易被忽略卻最卡關」），並用一句收斂：同樣 N 天，合併只看到『慢』，攤開才指得出卡在哪一段。\n時間軸與長條用手寫排版（SVG 或 div 比例條），react state 管理情境與視角，motion 控制過場；\n不用 emoji（一律 lucide-react icon），尊重 prefers-reduced-motion。"} caption="時間欄位愈細，瓶頸愈清楚：切換情境與「合併／攤開」視角，看同樣的總耗時如何指出不同的卡點">
  <TimeFieldsBottleneck client:visible />
</GeneratedFrame>

## 發現瓶頸後的下一步

時間欄位幫你**指出瓶頸卡在哪一段**，但這只是第一步。更現實的問題是：很多時候，瓶頸根本不會有人主動講出來。

一般工程師常常埋頭苦幹，也容易報喜不報憂（其實就是在說我自己 🤡）——卡關了不一定會主動反應，問起來還會說「再一下就好」，結果一路拖到火燒屁股、一發不可收拾才被發現。等到那時候，往往已經錯過最便宜的處理時機。

所以瓶頸數據真正的價值，是讓 PM **不必等人開口就能及早發現異常**：

- **看趨勢、不只看單點**：某一段的耗時連續幾次都在拉長，就算當事人說「沒事」，數據已經先示警了。
- **主動把問題攤開來談**：與其等對方求救，不如 PM 拿著數據先問「這段最近卡比較久，是不是遇到什麼困難？」——把「報憂」的責任從個人轉移到流程上。
- **早一點介入、便宜一點解決**：瓶頸越早抓到，可動用的選項（補人、調範圍、改順序）越多；拖到最後，常常就只剩硬扛一條路。

:::tip
工具與數據不會自己解決問題，它們的作用是**把「靠運氣發現」變成「靠機制發現」**——尤其當團隊裡有像我這種不太會主動喊卡的人時，這層保險特別重要。
:::

**發現瓶頸**之後，四種瓶頸的**病因不同、藥方也就不同**。下面這張互動處方手冊，把前置擁塞 / 估時失準 / 驗收塞車 / 上線延宕逐一拆開：點 tab 切換情境，再沿「介入前 → 介入中 → 介入後」三階段，依序看到察覺症狀、診斷病因、開立處方。

{/* @ai-visualize
id: bottleneck-playbook
type: free
prompt: |
  做一個互動式「瓶頸診療室 / 介入處方手冊」，接在「發現 Bottleneck 後的下一步」段落最後。
  核心洞察：四種瓶頸病因不同、藥方就不同；而且不管哪一種，越早介入、能用的處方越多、越便宜。
  此元件承接上方的時間欄位視覺化——上方回答「瓶頸卡在哪一段」，這裡回答「找到後怎麼介入、怎麼解」。

  版面：
  - 頂部一條任務生命週期迷你時間軸（建立 → 開始 → 完成 → 驗收 → 上線），四個區間對應四種瓶頸，
    當前選中的區間高亮、其餘以中性藍淡化，視覺語言與上方 time-fields-bottleneck 一致。
  - 中間四選一 tab（前置擁塞 / 估時失準 / 驗收塞車 / 上線延宕）。
  - 下方診療卡，內容隨「介入階段」累進揭露：症狀（看板上長怎樣）、病因（root cause）、
    處方（2–3 條 PM 介入手段）；底部恆顯「越早越便宜」（拖久的連鎖後果收斂句）。

  關鍵互動：以「介入前 → 介入中 → 介入後」三段 stepper 串成診療敘事，依序揭露——
  介入前只察覺「症狀」、介入中才理解「病因」、介入後才開出「處方」。
  時間軸上的瓶頸段隨階段變化：介入前紅（瓶頸）、介入中琥珀（診斷中）、介入後轉綠並 motion 平滑縮短（已疏通），
  處方項目逐條 stagger 亮起打勾。讓讀者親身走一遍「發現 → 診斷 → 處置」的過程。

  以 react state + motion 驅動（200–400ms ease-out，useReducedMotion 尊重 prefers-reduced-motion）；
  禁止 emoji，狀態 / 方向語意一律用 lucide-react icon；色票、字級、間距、圓角遵循 trendlink-design，勿硬編碼色碼。
caption: 四種瓶頸的介入處方手冊：點 tab 切換情境，沿「介入前 → 介入中 → 介入後」依序看見症狀、病因與處方。
status: generated
*/}

import BottleneckPlaybook from '@/components/generated/bottleneck-playbook'

<GeneratedFrame id="bottleneck-playbook" type="free" prompt={"做一個互動式「瓶頸診療室 / 介入處方手冊」，接在「發現 Bottleneck 後的下一步」段落最後。\n核心洞察：四種瓶頸病因不同、藥方就不同；而且不管哪一種，越早介入、能用的處方越多、越便宜。\n此元件承接上方的時間欄位視覺化——上方回答「瓶頸卡在哪一段」，這裡回答「找到後怎麼介入、怎麼解」。\n\n版面：\n- 頂部一條任務生命週期迷你時間軸（建立 → 開始 → 完成 → 驗收 → 上線），四個區間對應四種瓶頸，當前選中的區間高亮、其餘以中性藍淡化，視覺語言與上方 time-fields-bottleneck 一致。\n- 中間四選一 tab（前置擁塞 / 估時失準 / 驗收塞車 / 上線延宕）。\n- 下方診療卡，內容隨「介入階段」累進揭露：症狀（看板上長怎樣）、病因（root cause）、處方（2–3 條 PM 介入手段）；底部恆顯「越早越便宜」（拖久的連鎖後果收斂句）。\n\n關鍵互動：以「介入前 → 介入中 → 介入後」三段 stepper 串成診療敘事，依序揭露——介入前只察覺「症狀」、介入中才理解「病因」、介入後才開出「處方」。時間軸上的瓶頸段隨階段變化：介入前紅（瓶頸）、介入中琥珀（診斷中）、介入後轉綠並 motion 平滑縮短（已疏通），處方項目逐條 stagger 亮起打勾。讓讀者親身走一遍「發現 → 診斷 → 處置」的過程。\n\n以 react state + motion 驅動（200–400ms ease-out，useReducedMotion 尊重 prefers-reduced-motion）；禁止 emoji，狀態 / 方向語意一律用 lucide-react icon；色票、字級、間距、圓角遵循 trendlink-design，勿硬編碼色碼。"} caption="四種瓶頸的介入處方手冊：點 tab 切換情境，沿「介入前 → 介入中 → 介入後」依序看見症狀、病因與處方。">
  <BottleneckPlaybook client:visible />
</GeneratedFrame>

---

## 小結

這幾項工具不是各做各的，而是互相輔助：

| 工具 | 在流程中的角色 | 我的使用情境 |
| --- | --- | --- |
| **WBS** | 把交付物**由上而下拆解**，界定整個專案的範圍 | 專案啟動、要確認「做什麼、不做什麼」時先攤開 |
| **工作包** | WBS 的**末端葉節點**，範圍的最小單位（以成果命名） | 拆到「能被一個人扛起、驗收標準明確」就停（呼應 8/80） |
| **Gantt** | 把工作包**排上時間軸**，看起訖、依賴與整體時程 | 我習慣直接在 WBS 上加開始日 / 工時欄位反推時程 |
| **Kanban** | 追蹤展開後的 **Task 流動狀態**，讓瓶頸現形 | 日常推進看哪一欄堆積，反推卡點（通常是驗收） |
| **Task** | 工作包展開出的**執行單元**，記錄人 / 時間 / 狀態 | 用細緻的時間欄位反推每段耗時、校正估時 |

:::tip
實務上我不會把這四套工具當成四份獨立文件來維護，而是按層級區分：整個專案的運行全貌，使用 WBS 加上時間欄位就同時是 Gantt；末端實際執行時使用 Kanban 與 Task 紀錄。

- 範圍與時程（WBS / 工作包 / Gantt）偏**規劃期**使用。
- 流動與紀錄（Kanban / Task）偏**執行期**使用。
:::