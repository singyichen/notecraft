---
title: Markdown 擴充語法示範
description: NoteCraft 支援的 Admonitions、Content tabs 與 Tooltips 寫法與效果示範。
tags:
  - 基本操作
category: 系統說明
createdAt: '2026-06-16'
updatedAt: '2026-09-25'
---

NoteCraft 在標準 Markdown / MDX 之外，提供六種類 Material for MkDocs 的內容增強語法——Admonitions、Content tabs、Tooltips、Badge、Choices、Steps——皆以 `remark-directive` 於 build 階段渲染、樣式遵循 trendlink-design，正式環境同樣可用。另有程式碼區塊增強（見本頁最後一節），由 `astro-expressive-code` 負責。

## Admonitions（提示框）

用 `:::類型` 容器標記，支援 `note` / `info` / `tip` / `success` / `warning` / `danger`。

:::note
這是 **note** 提示框，內含 [連結](/notes) 與 `行內程式碼` 都正常渲染。
:::

:::warning{title="自訂標題"}
帶 `{title="…"}` 可自訂標題列文字。
:::

:::tip{collapsible}
帶 `{collapsible}` 會渲染為預設收起的 `<details>`，不需任何 JavaScript 即可展開 / 收合。
:::

:::danger
危險 / 注意事項用 `danger`。
:::

## Content tabs（內容分頁）

用 `::::tabs` 包住多個 `:::tab{label="…"}`；切換以框架無關的 vanilla JS 實作，停用 JS 時會展開所有面板、不遺失內容。

> 巢狀容器規則：外層 `tabs` 要比內層 `tab` **多一個冒號**（`::::tabs` 包 `:::tab`），remark-directive 才能正確區分巢狀層級。

::::tabs
:::tab{label="npm"}
```bash
npm install notecraft
```
:::
:::tab{label="pnpm"}
```bash
pnpm add notecraft
```
:::
:::tab{label="yarn"}
```bash
yarn add notecraft
```
:::
::::

## Tooltips（行內提示）

用 `:tip[可見文字]{content="說明"}` 標記，滑鼠移上或鍵盤聚焦即顯示：像 :tip[OAuth]{content="一種授權框架，讓第三方應用在不取得密碼的情況下存取資源。"} 這樣。

## Badge（行內標籤）

用 `:badge[文字]{variant outline size icon href}` 行內 directive 標記。`variant` 與 Admonitions 共用同一套語意色（`note` / `info` / `tip` / `success` / `warning` / `danger` / `neutral`）。

- 預設 solid：:badge[新]{variant="success"} :badge[Beta]{variant="warning"} :badge[必填]{variant="danger"} :badge[v1.8.0]
- 加 `outline` 旗標：:badge[note]{variant="note" outline} :badge[info]{variant="info" outline} :badge[neutral]{variant="neutral" outline}
- 帶 icon：:badge[新功能]{variant="success" icon="sparkle"} :badge[已完成]{variant="success" icon="check"} :badge[警告]{variant="warning" icon="warning"}
- `size="sm"`（適合表格 cell 內）：:badge[必填]{variant="danger" size="sm"} :badge[選用]{variant="neutral" size="sm" outline}
- 可點擊（有 `href`）：:badge[查看 Admonitions]{variant="info" href="#admonitions"} :badge[GitHub]{variant="neutral" icon="star" href="https://github.com/"}

## Choices（選擇題選項）

用 `:::choices{answer="B"}` 容器包一份 Markdown 清單，字母 A／B／C… 依清單順序自動指派——選項本身不必自己編號，重排選項時只要改 `answer`。正解整列以 tip 語意色標出，並附勾號與視覺隱藏的「正解」文字供螢幕閱讀器朗讀。

:::choices{answer="B"}
- 會損失該類別欄位的資訊
- 被刪的那一欄是其餘欄位的線性組合
- 只是為了節省記憶體
- 只有樹模型需要這樣做
:::

`answer` 可用逗號分隔標多個正解（複選題），大小寫不分：

:::choices{answer="a, c"}
- 空乏區的寬度會隨逆偏電壓增加而變寬
- 順偏時空乏區變寬
- 導通電壓（knee voltage）約在 0.7 V 附近
- 逆偏電流與溫度無關
:::

寫法出錯時只會在 build log 留 warn 並退化為不標記的一般清單，不會讓 build 失敗，也不會讓選項消失（`answer` 指到不存在的字母、或忘了寫 `answer` 都是如此）。

## Steps（步驟）

用 `::::steps{layout}` 容器包多個 `:::step{title status}`，預設 `vertical`。`status` 三態：`done` / `current` / `todo`（預設 `todo`）。

### Vertical（預設）

:::::steps
::::step{title="安裝依賴" status="done"}
執行 `npm install`，等待依賴下載完成。
::::
::::step{title="設定環境變數" status="current"}
複製 `.env.example` 為 `.env`，填入 :badge[必填]{variant="danger" size="sm"} 欄位。

:::tip
記得把 `.env` 加入 `.gitignore`，避免外洩金鑰。
:::
::::
::::step{title="啟動 Dev Server"}
`npm run dev` 後開啟 `http://localhost:4321`。
::::
:::::

### Horizontal

`layout="horizontal"`；視窗 < 640px 時自動降級為 vertical（內建、作者無法關閉）。

::::steps{layout="horizontal"}
:::step{title="計劃" status="done"}
釐清需求與待釐清項。
:::
:::step{title="設計" status="done"}
產出規格與 task。
:::
:::step{title="實作" status="current"}
依 task 撰寫元件與樣式。
:::
:::step{title="驗收"}
跑 `astro build` 確認通過。
:::
::::

### 自訂起始序號

::::steps{start=3}
:::step{title="第三步"}
從 `03` 開始計數。
:::
:::step{title="第四步"}
接著是 `04`。
:::
::::

## 程式碼區塊增強（Code blocks）

圍欄程式碼於 build 階段渲染為白底程式碼塊：語言標籤、複製按鈕、固定行號欄、整行高亮與可展開的程式碼註解。

### 行號 + 整行高亮

每個程式碼塊都有固定行號欄；在 fence 的語言後加 `{行號}`（如 `{3,6-7}`、1-indexed）即可標出重點行：

```ts {3,6-7}
export function signIn(user: string, token: string) {
  if (!token) {
    throw new Error("missing token"); // 第 3 行被 highlight
  }
  const session = createSession(user);
  session.token = token; // 第 6–7 行被 highlight
  return session;
}
```

### 檔名標題

加上 `title="路徑/檔名"` 會在標題列的語言標籤旁顯示檔名（可與 `{行號}` 並用）：

```ts title="src/lib/auth.ts" {2}
import { createSession } from "./session";
const token = readToken(); // 第 2 行被 highlight
export const session = createSession(token);
```

### Code annotations（可展開註解）

用 `:::annotate` 容器包住「程式碼 + 編號清單」，程式碼中以 `(n)!` 標記，點擊標記即浮現對應說明（停用 JS 時清單依序顯示、不遺失內容）：

:::annotate
```ts
function handler(req: Request) { // (1)!
  validate(req);
  return ok(req); // (2)!
}
```

1. 進入點，先校驗請求格式是否合法。
2. 回傳標準化的成功包裝物件。
:::
